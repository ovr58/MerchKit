/**
 * Публичный интерфейс модуля `catalog-taxonomy` (docs/SPEC.md §3): справочники генерации.
 * Откуда они берутся и как кэшируются — внутри модуля.
 */

export {
  FREEFORM_CATEGORY,
  presetsOf,
  profileOf,
  titleOf,
  useTaxonomy,
  type Category,
  type Marketplace,
  type OutputProfile,
  type Preset,
  type Taxonomy,
} from './api'
