# Разбор карточки в гайд «арт-директора» → послойный скрипт сборки

> **Заметка пользователя, записана 2026-08-31.** Исходник — `my_description.txt` в корне
> репозитория, перенесён сюда без правок текста. Это **сырьё для вехи M7**
> ([`planning/active/card-assembly-pipeline_2026-08-31.md`](../active/card-assembly-pipeline_2026-08-31.md)),
> а не утверждённая спецификация: формулировки языка макета и состав слоёв берутся из плана,
> здесь — исходная постановка в словах пользователя.

---

Карточка разбирается на гайд для "арт директора"
Пример гайда:
Фон - размытое изображение, описание изображения: "осенний пейзаж, деревья без листьев и так далее", степень эффекта размытия - такая то, товар на модели - пуховик-куртка, модель - идет описание модели, логотип компании производителя (либо селлера), в фирменных цветах - при подборе фона обратить внимание на контраст и различимость белого логотипа на нашем размытов фоне, слева отцентрованный по вертикали блок описания товара и его преимуществ, сейл тек, название товара таким то шрифтом таким то размером, такой то толщины (перечислить все параметры шрифта), ниже гендерная принадлежность товара - важно в некоторых случаях - для одежды, ниже значек темпиратуры, рядом с которым текст темпиратурного режима ношения куртки, ниже табличка с полупрозрачным фоном черного цвета степень прозрачности 50% с белой рамкой толщиной 5 px, в табличке надпись "Для походов" - полностью помещается в табличку и имеет отступы от краев сверху такой то снизу такой то слева такой то и справа такой то пропорции от соответственно длины и высоты, табличка слева имеет четко видный размером в пропорции к высоте таким то значек белого плюса на оранжевом фоне в кружке, который имеет белую обводку толщиной 8 px, с соблюдением основных параметров ниже идет такая же по дизайну табличка увеличенная по высоте пропорционально в которой с выравниванием по левой стороне "Пропитка", под этим словом логотип фирмы такой то на белом - в результате из этого описания имее инструкцию которая грубо в приближении должна представлять из себя скрипт (как есть скрипты в фотошопе) запуск которго приводит к тому что у нас будет появлятся фон на нулевом слое сгенерированный нейронкой улчшенный и масштабированный соответственно, потом на первом слое сгенерированное нейронкой изображение модели в товаре в соответствии с требованиями макета (отступы и прочее), затем добавляется слой иконок табличек и потом слой редактируемого текста с соответствующими шрифтами, для получения таких параметров скрипта как отступы, компановка объектов, размеры шрифтов и уровни прозрачности - сначала просим сгенерировать с нашими данными новую карточку в таком то размере с параметрами описания из которой с помощью описателя достаем нужные параметры для скрипта - в результате пользователь может все двигать удалять и менять на полученном изображении
