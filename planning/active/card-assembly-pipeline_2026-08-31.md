# Конвейер сборки карточки слоями

Status: ACTIVE (с 2026-08-31)

## Context

**Проблема.** Карточка сейчас рисуется одним вызовом модели изображений: она же рисует сцену,
она же выводит текст. Отсюда весь хвост дефектов — брак текстового блока (B11 → ADR-0011,
остаточный риск ~1 кадр из 7), невозможность вывести характеристики товара, невозможность
правки без перерисовки (B14), вынужденный дорогой бакет разрешения ради четырёх пикселей
порога Ozon. Дизайна в результате нет: есть фото и подпись.

**Что выяснено разведкой 2026-08-31** (Fabula AI, PicCopilot, Neiro-Card.AI): ни один
работающий сервис этого класса не просит генеративную модель рисовать текст. У всех троих
конвейер разделён — модель отвечает за пиксели сцены, вёрстка накладывается отдельным слоем
по заранее заготовленному макету. «Уникальная, не шаблонная инфографика» в их маркетинге —
это параметризация макетов, а не генерация дизайна с нуля.

**Что решил пользователь 2026-08-31** — полный разбор в разделе «Решения пользователя»
ниже. Коротко: строим ту же схему, образцы отбирает пользователь по топу выдачи площадок,
свойства товара извлекаются из его промпта дешёвой текстовой моделью и правятся руками,
редактор выносится в отдельное приложение за неактивной кнопкой, пополнение баз иконок и
шрифтов — отдельный офлайн-конвейер.

**Ожидаемый исход.** Текст в карточке становится детерминированным (не «почти те же буквы»,
а ровно те), характеристики товара выводятся модулями, повторная сборка бесплатна, размер
файла точно соответствует профилю площадки, а качество дизайна определяется отобранными
образцами, а не удачей броска.

**Артефакт-объяснение для заказчика:** [V-11](../../docs/VISUALS.md#v-11) —
https://claude.ai/code/artifact/a6e753e8-8f25-4d98-9786-01a01ce98c25

## Цель

Заменить «модель рисует карточку целиком» на конвейер: разобранные образцы → макет как
данные → детерминированная сборка слоями. Довести до прохождения главного сценария на живом
вендоре с пилотной библиотекой макетов.

## Решения пользователя (2026-08-31)

Зафиксировано дословно по смыслу, чтобы холодная сессия не переспрашивала.

| № | Вопрос | Решение |
| --- | --- | --- |
| 1 | Кто отбирает образцы и сколько | Отбирает пользователь. По 10 на категорию, источник — топ выдачи площадки |
| 2 | Чужие образцы или свои | Чужие на старте; ось «маркетплейс» пользователь предложил как деление библиотеки — см. вопрос O-2 |
| 3 | Подбор макета | По разобранному промпту пользователя: «товар на модели» → макеты с моделями, упомянут логотип → макеты с логотипом, описан фон → макеты с генерируемым фоном |
| 4 | Откуда характеристики | Из описания товара, которое пишет пользователь. Дешёвая текстовая модель извлекает список свойств и ранжирует по важности; пользователь меняет порядок, удаляет и дописывает. Нет свойства — модуль занимает другое свойство или не рисуется вовсе |
| 5 | Логотип | Упомянут в промпте → макеты с логотипом + отдельное поле загрузки PNG со строгой проверкой. Пользователь не загрузил → слой логотипа не рисуется, и он об этом предупреждён |
| 6 | Фон и товар — одной генерацией или двумя | Раздельная схема допускается: фон генерируется первым и уходит референсом в генерацию товара с прямым указанием согласовать свет. См. коррекцию K-2 — для нашей задачи достаточно одной |
| 7 | Человек в кадре | Приоритетный сценарий. Риск анатомии рук замерить. См. ответ в K-4 |
| 8 | Иконки | База пополняется офлайн-конвейером при разборе образцов: имя, описание, содержимое (SVG или base64). Отдельная процедура заполняет содержимое. Нет иконки — см. коррекцию K-3 |
| 9 | Шрифты | Скачиваются с источников со свободной лицензией, ведутся списком в базе как иконки. Разнообразие диктуется образцами |
| 10 | Что отдаём | Только готовый файл. Кнопка «Редактировать» присутствует, но неактивна — готовность к подключению стороннего редактора |
| 11 | Глубина правок | Полные правки (позиция, размер, текст, порядок слоёв, удаление объектов) выносятся в отдельное приложение. В этой вехе — только «переписать текст и пересобрать», бесплатно |
| 12 | Промежуточная генерация | Не ради чисел, а ради (а) уникальности вёрстки и (б) проверки на нестыковки макета с содержимым. См. коррекцию K-1 |
| 13 | Критерий красоты | Отложен: дизайнеры подключаются на коммерческой основе при наличии прибыли, как часть продуктовой итерации. В этой вехе критерий приёмки — механический (см. Verification) |
| — | Порядок и число слоёв | **Не фиксированы.** Логотип может лежать перед моделью и наоборот. Это прямо подтверждает, что макет — список слоёв с z-порядком, а не жёсткий шаблон |
| — | Три расхождения из разбора | Приняты все три: сборщик — программа, а не модель; промежуточная генерация не обязательна; вырез товара идёт раньше арт-директора |

## Коррекции механики

Четыре места, где реализация отличается от буквального прочтения решений выше. Каждая —
удешевление или снятие риска, ни одна не сужает продукт.

### K-1. Проверка нестыковок — бесплатным превью, а не платной генерацией

Цель верна: пользователь должен увидеть, что вёрстка съедет из-за его вводных, **до** того
как заплатит. Но платная генерация карточки для этого не нужна и хуже справляется.

У сборщика есть все числа макета, значит переполнение вычисляется арифметикой, а не
угадывается по картинке: строка длиннее бокса — это факт, а не впечатление. Сборщик рисует
превью макета мгновенно, бесплатно и на заглушке вместо фото товара. Пользователь правит
свойства и порядок, и только потом уходит платный вызов.

Промежуточная генерация как «режим вдохновения» (прочтение «а») остаётся расширением за
пределами вехи — сам пользователь отнёс такие усложнения в отдельное приложение.

### K-2. Вырез товара не требует раздельной генерации

Приём «текст частично уходит за товар» требует товара с прозрачным фоном — но **не** требует,
чтобы фон и товар рисовались разными вызовами. Достаточно одной генерации сцены и вырезания
товара из неё: перекрытие получается, а свет и перспектива сходятся сами, потому что сцена
рисовалась целиком.

Раздельная генерация нужна только чтобы двигать товар относительно фона, а этого макет не
требует. Экономим второй платный вызов и снимаем риск «аппликации».

Формулировка согласования света — на случай, если раздельная схема всё же понадобится
(готова, в дело не идёт):

> «Фон задан приложенным изображением. Источник и направление света, высота солнца,
> температура света и мягкость теней на товаре обязаны совпадать с ним: освещённая сторона
> товара — та же, что освещённая сторона сцены; тень товара падает в ту же сторону и с той
> же мягкостью, что тени на фоне.»

### K-3. Плейсхолдер иконки — в офлайн-конвейер, не в пользовательский результат

Двойной клик по плейсхолдеру в готовом файле — это уже редактор, а редактор решением 11
вынесен из вехи. Развязка: **иконки нет в базе → модуль рисуется без иконки, текст
остаётся**, композиция не ломается и пользователь ничего не чинит. Загрузка недостающих
иконок — административная процедура офлайн-конвейера.

Для логотипа решение 5 уже даёт то же поведение: не загрузил — слой не рисуется. Правило
одно на оба случая: **отсутствующий ассет убирает свой слой, а не ломает макет.** Это
требование к валидатору макета, а не к интерфейсу.

### K-4. Руки в кадре: стало лучше, но не «уже не проблема»

Прямой ответ на вопрос. Лишние пальцы на моделях уровня `gpt-image-2` встречаются заметно
реже, чем два года назад, но дефект не исчез — он сместился: реже шестой палец, чаще
искажение кисти на стыке с предметом, асимметрия обуви и неверные складки на месте сгиба.
Автоматической проверки анатомии у нас не будет и быть не может.

Два практических хода. Первый — замерить на нашей модели и знать частоту (шаг C2).
Второй — дизайнерский, и он бесплатный: **предпочитать макеты, где кисти рук не в фокусе.**
В самом образце, с которого начался разбор, одна рука убрана в карман, вторая наполовину
закрыта плашкой «Для походов». Это не совпадение, а стандартный приём предметной съёмки, и
он же снижает наш риск. Признак «кисти скрыты» заводится тегом образца при разборе.

### Уточнение к решению 3, не коррекция

«Пользователь пишет промпт» в нашем интерфейсе — это не одно поле. По FR-08 он выбирает
сценарий показа из четырёх готовых, по FR-09 дописывает свободные пожелания, плюс есть
категория из FR-03 и загруженный логотип. Признаки для подбора макета собираются из всех
четырёх источников, а не только из свободного текста.

### Замечание по отбору образцов, не блокирующее

Топ выдачи площадки — хороший источник кандидатов, но он ранжирует по продажам, отзывам,
рекламной ставке и логистике; дизайн карточки влияет на позицию лишь косвенно. То есть топ
даёт не «уже отобранные по всем критериям» карточки, а хорошую выборку для ручного отбора —
которую пользователь и делает сам. На процедуру это не влияет, но обоснование стоит держать
верным: отбор остаётся вкусовым решением, и потолок качества продукта равен этому отбору.

## Интеграция в цикл планирования

Разбор накладок с действующими вехами и бэклогом — по требованию «без накладок».

### Что этот план поглощает и отменяет

| Запись | Что с ней |
| --- | --- |
| **B15** «Дизайн карточки: референсы по категориям» | **Поглощается целиком.** B15 чинит старую схему: подкладывает образцы в промпт, чтобы модель скопировала стиль. В новой схеме стиль задаём мы, копировать нечего. Замер «референсы вкл/выкл» (12 кадров, 30–45 ₽) не нужен — **экономия платного замера и одной сессии** |
| **B14** «Правка карточки промптом за баллы» | **Сжимается.** Текстовая правка становится бесплатной пересборкой (решение 11) и уходит в шаг B7. Замер «сохранности кадра на 10 правках» (~100 ₽) для неё не нужен. В бэклоге остаётся только платная перерисовка картинки |
| **B9** «Зависимости в Edge Functions» | **Закрывается как разовый сбой** — проверено 2026-08-31, см. «Замеры фазы A». Блокером быть перестал, запасной путь для A2 не понадобился |

### Что этот план разблокирует

**Главный блокер M6 — формулировки 24 сценариев показа.** Разбор образца даёт не только
макет, но и **сценарий показа**: «на модели, осенний пейзаж, средний план» — это ровно тот
текст, который FR-08 требует как предсозданный сценарий. Один разбор → два результата.
Пользователь всё равно отбирает по 10 карточек на категорию; сценарии набираются попутно и
из тех же материалов. Отдельной работы с контентом для M6 не остаётся.

### Порядок вех

M6 «Наполнение и приёмка» — приёмка продукта. Принимать механизм карточки, который эта веха
заменяет, — работа впустую: все прогоны FR-07 и US-E4 придётся повторять. Рекомендация —
**этот план идёт вехой M7 перед M6**, а наполнение сценариями переезжает в него из M6. M6
остаётся чистой приёмкой и запускается последней, по уже неизменному механизму.

Решение о перестановке вех — за пользователем (вопрос O-1).

### Что идёт параллельно и не конфликтует

- **B13** — про надёжность вызова вендора. Фаза A живого вендора не касается вовсе, поэтому
  идёт параллельно. **Формулировка записи исправлена 2026-08-31:** «треть генераций не
  доходит до кадра» измерена при потолке 60 с и старой модели, с тех пор потолок 120 с
  (ADR-0011) и модель другая, а разрыв в 32 с не воспроизвёлся. Осталось два вопроса:
  ретрая нет ни одного, и частота отказов на нынешнем стеке не мерена. **Фаза C без ответа
  по ним бессмысленна**, но начинается ответ с замера, а не с диагностики.
- **B12** (распознавание за 6,6 с при бюджете 5 с) — независимо, пересечений нет.
- **B16** (переиздать артефакт V-09) — независимо.

## Критичные файлы

- `supabase/functions/_shared/ai-provider/aitunnel.ts` — `imagePrompt`, `cardLayoutLine`,
  `cardReferenceParts`, `input_references`. Отсюда уходит вёрстка текста
- `supabase/functions/_shared/ai-provider/types.ts` — `CardTexts`, `ProductBrief`,
  `OutputProfile`; сюда приходят свойства товара
- `supabase/functions/_shared/output-profile.ts` — сейчас профиль только проверяется;
  сборщик должен к нему **приводить**
- `supabase/functions/generation-worker/index.ts` — точка, где встаёт сборка
- `docs/assets/cardsforsysprompt/` — три образца, с которых начинается round-trip
- `docs/design/d1-account/gen.mjs` — рабочий пример «дизайн как код», техника переиспользуется
- `src/index.css` — токены с выверенным контрастом AA
- `planning/reference/MARKETPLACE_IMAGE_REQUIREMENTS.md` — профили площадок

## Шаги

### Фаза A — офлайн-конвейер образцов

Живого вендора не касается, баллов не тратит, идёт параллельно диагностике B13.

- [x] **A1. Язык макета — готов 2026-08-31.**
  [`supabase/functions/_shared/card-layout/types.ts`](../../supabase/functions/_shared/card-layout/types.ts)
  (схема) и [`validate.ts`](../../supabase/functions/_shared/card-layout/validate.ts)
  (валидатор + правило K-3), 12 тестов в `validate.test.ts`. Решение записано в
  [ADR-0012](../../docs/adr/0012-card-layout-is-ours-not-vendors.md); комментарий к
  `OutputProfile` в `ai-provider/types.ts` помечен как развёрнутый в части вёрстки.
  Закрытые словари: 6 типов слоёв (`frame`, `cutout`, `shape`, `asset`, `text`, `group`),
  3 эффекта (`shadow`, `blur`, `stroke`), 3 вида заливки (`solid`, `linear`, `radial`),
  3 режима наложения, 5 ролей шрифта, 6 текстовых гнёзд. Z-порядок — поле, а не позиция в
  массиве.
  **Два слова словаря добавлены вторым проходом, по замечаниям пользователя к гейту** — см.
  A3 ниже. Это и есть заявленный порядок работы: словарь растёт по найденной нехватке, а не
  «на будущее».
- [x] **A2. Сборщик — готов 2026-08-31.**
  [`svg.ts`](../../supabase/functions/_shared/card-layout/svg.ts): макет + содержимое → SVG
  точного размера, кадр вендора вложен как `<image>` с data-URI. Растеризация вынесена из
  общего кода — это единственное место, зависящее от рантайма.
  **Развилка рантайма закрыта, и не выбором из двух, а тем, что выбирать оказалось не из
  чего:** растеризатор — `resvg` в виде WebAssembly, один и тот же модуль в Deno, браузере и
  Node. Значит SVG считается общим кодом везде, а «Edge Function против клиента» становится
  вопросом места запуска, а не второй реализации. Основное место — Edge Function
  (`generation-worker`), клиент остаётся запасным без переписывания.
  **B9 воспроизвести не удалось — и это проверено, а не предположено** (см. ниже).
- [x] **A3. Round-trip — гейт пройден 2026-08-31.** Разобраны все три образца из
  `docs/assets/cardsforsysprompt/`, собраны по собственному разбору, результат положен рядом
  с оригиналами: `*.rebuilt.png` (наши слои поверх оригинала — проверка попадания) и
  `*.layers.png` (слои без кадра — проверка полноты). Инструмент —
  [`tools/card-pipeline/`](../../tools/card-pipeline/), запуск `npm run cards:roundtrip`.
  Язык выразил все три композиции без расширения словаря, включая переменный z-порядок
  (в образце «футболка» модель лежит **поверх** строки навигации), модули-группы, градиентную
  колонку, образцы цвета и вылет слова-подложки за обрез.
  **Отбор 21 образца (A6) начинается только после подтверждения гейта пользователем.**
  Страница для решения — [V-13](../../docs/VISUALS.md#v-13):
  https://claude.ai/code/artifact/0c826cf1-2e51-4fb8-91af-2095b2b350c6

  **Второй проход 2026-08-31 по замечаниям пользователя.** Первый прогон гейт формально
  прошёл, но пользователь нашёл в нём пять расхождений — и две из них оказались дырами в
  языке, а не промахами разбора. Ради этого гейт и стоит перед набором библиотеки: найди мы
  их после 21 образца, переписывать пришлось бы 21 разбор.

  | Замечание | Что это было | Что сделано |
  | --- | --- | --- |
  | Плашка размеров на «платье» не ровно белая, а гаснет к краям | **Дыра в языке:** заливок было две, обе линейные | Добавлена заливка `radial` — падение к краям тянется вместе с плашкой, а не остаётся круглым пятном |
  | У плашек «куртки» светлая волосяная обводка, и кружок «+» посажен ровно на неё | **Дыра в языке:** `stroke` был объявлен на A1 и убран как «не нужен ни одному образцу» — ошибка разбора, выданная за YAGNI | `stroke` возвращён и **реализован** (у текста — с `paint-order`, иначе обводка съедает штрих глифа) |
  | Колонка на «платье» не заливка, а размытая затемнённая фотография | Промах разбора; язык это уже умел | Колонка стала слоем `frame` по левому краю кадра с эффектом `blur` плюс полупрозрачный тёмный скрим поверх |
  | Плашки «куртки» тёмные с белым текстом, а не белые с тёмным | Промах разбора | Заливка и цвет текста исправлены |
  | Иконка температуры заметно мельче оригинальной | Промах разбора: квадратный `viewBox` иконки при `contain` заполнял бокс наполовину | Иконка перерисована с вытянутым `viewBox`, бокс приведён к пропорции оригинала |

  Отдельно: **«+ADD SHIELD» — фирменный знак технологии, то есть второй бренд-ассет
  продавца, а не характеристика товара.** Замена его текстом-значением работает, но подложка
  под ним не должна оставаться в кадре, когда знака нет. Записано как под-группа,
  привязанная к тому же значению: нет знака — нет и подложки. Продуктовый вопрос «откуда
  такой знак берётся» — новый **O-7**.
- [x] **A4. Разбор образца — процедура готова 2026-09-01.**
  [`tools/card-pipeline/parse.mts`](../../tools/card-pipeline/parse.mts), запуск
  `npm run cards:parse -- submit | status | fetch`. Vision-модель по образцу отдаёт макет по
  схеме A1, содержимое и примечания; шрифты опознаются **ролью**, не именем — гарнитуру по
  растру достоверно не восстановить и чаще всего не лицензировать.

  **Batch API вместо обычных вызовов.** Разбор офлайновый, ответа никто не ждёт, а батч стоит
  вдвое дешевле: проход по 31 образцу — около $5. Это решает, потому что проходов заведомо
  несколько: дыры в языке находятся уже после разбора.

  **Вендор здесь не продуктовый.** Скрипт ходит в Anthropic напрямую, минуя `ai-provider`:
  ADR-0005 держит вендора продукта подальше от продуктового кода, а этот в рантайм не
  попадает — инструмент разработчика, как `assets.mts`. Ключ и рабочее пространство описаны
  в `.env.example`.

  **Задание не дублирует язык:** собирается из самого `types.ts` и готового разбора-примера,
  поэтому разъехаться с реализацией ему нечем.

  **Проверка на границе двойная.** Макет — `validateLayout`, тем же валидатором, что и у
  сборщика. Содержимое — своей проверкой в скрипте: продуктовый валидатор `content` не
  смотрит и правильно делает (в продукте тот приходит из нашего кода по типам), а здесь он
  приходит от модели. Один разбор из 31 положил прогоны в `content.props[].label` вместо
  макета и уронил сборку на `.split` — теперь такой ответ отклоняется с внятным сообщением.

  **Сценарий показа и теги для подбора (FR-08) этим шагом НЕ закрыты** — разбор отдаёт макет,
  содержимое и примечания. Теги переезжают в фазу B вместе с подбором макета (шаг B2).
- [x] **A5. Базы иконок и шрифтов — готово 2026-08-31.** Четыре таблицы миграцией
  [`20260831120000_card_assets.sql`](../../supabase/migrations/20260831120000_card_assets.sql):
  `card_icons`, `card_font_families`, `card_fonts`, `card_font_roles`. Дедуп по имени —
  первичным ключом и парой «гарнитура × насыщенность × наклон»; **дедуп по содержимому —
  уникальным индексом по `sha256`**, то есть ограничением базы, а не кодом, который можно
  забыть вызвать. Процедура заполнения — [`tools/card-pipeline/assets.mts`](../../tools/card-pipeline/assets.mts)
  (`npm run cards:assets` · `push` · `pull` · `request`), 12 тестов в
  `supabase/tests/database/card_assets.test.sql`.
  **Заявка и готовая запись — одна строка, а не два состояния в разных местах:** статус
  вычисляется из наличия содержимого генерируемой колонкой, поэтому разъехаться с фактом
  ему нечем. Форма записи — та, что объявлена витриной V-12 (шаг A7).
  **Шрифты только со свободной коммерческой лицензией**, и хранится не только её название:
  OFL требует распространять полный текст вместе с файлами, а раздаёт файлы отсюда сборщик —
  значит текст лежит полем у гарнитуры. Новая гарнитура заводится **миграцией**, а не
  скриптом: семья, роль и лицензия — решения, они обязаны проходить ревью в диффе.
  **Побочный эффект, который стоит назвать:** папки `icons/` и `fonts/` перестали быть
  базой и стали её рабочей копией. Списка файлов и таблицы «роль → гарнитура» в
  растеризаторе больше нет — он читает папку целиком и `roles.json`, который пишет `pull`.
  Проверено пересборкой: после переезда три образца дали **побайтово те же PNG**.
- [x] **A6. Стартовый набор — 31 образец с Wildberries, разобран 2026-09-01.** Набор —
  [`bench/samples/wb-starter/`](../../bench/samples/wb-starter/) (пользователь отобрал 31
  вместо ~21 и оставил все), разборы — `tools/card-pipeline/samples/wb-*.json`. Гейт собирает
  все 34 макета (31 A6 + 3 A3) без возражений валидатора.

  **Как шёл проход.** Пробник на одном образце → разброс на пяти непохожих категориях →
  правка языка по объединению дыр → полный проход. Разброс окупился прямо: он нашёл три
  ломающие дыры, и без него 31 разбор лёг бы на язык, который через час изменился. Полный
  проход дал 30 из 31 с первого раза; единственный провал оказался расхождением внутри
  нашего кода (валидатор запрещал плоский бокс линии, который сборщик рисует), а не промахом
  разбора.

  **Новые слова языка пошли в дело:** поворот в 12 слоях, прогоны в 20 строках, привязка ко
  второму кадру в 11, скругление в 17.

  **Находка для A5, не закрытая: имена иконок не переиспользуются между образцами.** 27
  уникальных имён на 31 образец, ни одного повтора — и среди них семейства-почти-дубли
  (`shirt` / `shirt-outline` / `tshirt-loose`, `leaf` / `leaf-breathable`, четыре разных
  `sole-*`). Дедуп базы из A5 их не поймает: он ловит совпадение имени и совпадение
  содержимого по `sha256`, а это разные имена у ещё не нарисованных иконок. Причина
  устранимая — каждый разбор придумывает имена независимо, не видя, что уже есть в базе.
  Напрашивается подача списка имён из базы в задание разбора; на пустой базе это было
  бессмысленно, при пополнении станет обязательным.

  Прежнее описание шага: **~21 образец, все с Wildberries** (решение пользователя
  2026-08-31, сужает прежний расклад «7 категорий × 3 площадки»). Отбирает пользователь сам,
  по нескольким разделам WB; разделов там много, и полный обход поля — работа для прода.
  **Цель набора прежняя — прогнать конвейер целиком, а не получить разнообразие.** Тот же
  объём в 21 образец, но ось «маркетплейс» на старте не покрывается: разбор проверяется на
  разных категориях одной площадки. Что это значит: требования площадок (метки в витрине A7)
  на этом наборе не отрабатываются — их проверка переезжает на пополнение, когда появятся
  образцы Ozon и Я.Маркета. Разнообразие и число добираются пополнением по ходу — ради этого
  офлайн-конвейер и строится как процедура, а не как разовая загрузка.
- [x] **A7. Витрина базы — первая версия готова 2026-08-31.**
  [V-12](../../docs/VISUALS.md#v-12), исходник `docs/design/card-library/`. Инвентарь трёх
  баз «папками с файлами»: 21 ячейка «категория × площадка» с метками требований площадки,
  полка неразобранного, пустые базы иконок и шрифтов с показанной формой записи, круг
  пополнения. Сделана по пустой базе намеренно — служит **спецификацией того, что
  офлайн-конвейер обязан отдавать**. Переиздаётся по тому же URL по мере наполнения:
  счётчики в шапке и статусы ячеек правятся в исходнике.

### Замеры фазы A (2026-08-31, бесплатные)

**B9 не воспроизводится.** Локальный рантайм функций (Supabase CLI 2.116.0,
`edge-runtime` 1.74.3) грузит зависимости всеми тремя способами: `jsr:`, `npm:` и прямой
`https://esm.sh`. Прямой `fetch('https://jsr.io/@std/uuid/meta.json')` **изнутри контейнера**
вернул 200 за 705 мс — TLS до jsr.io цел. Отрицательный контроль на месте: несуществующая
версия `npm:left-pad@9.9.9` отвалилась по «нет такой версии», а не по сети, то есть отвечал
настоящий резолвер, а не кэш. Сбой M1 был разовым.

**Растеризатор поднимается в рантайме функций.** `npm:@resvg/resvg-wasm@2.6.2` внутри
Edge Function: загрузка wasm 2,4 МБ — 2616 мс (однократно на холодный старт), `initWasm` —
7 мс, рендер кадра шириной 896 px — 83 мс, на выходе корректная сигнатура PNG. Это и есть
основание выбрать основным путём Edge Function, а не клиент.

**Переменные шрифты не годятся.** `resvg` берёт у переменного шрифта только экземпляр по
умолчанию: на Montserrat[wght] и Inter[opsz,wght] `font-weight="900"` и `font-weight="400"`
дали одинаковый растр. В базу шрифтов кладутся **статические начертания**, по файлу на
насыщенность. Стартовый набор — Montserrat (Regular / SemiBold / Bold / Black) и
Marck Script, обе SIL OFL с кириллицей, лицензии лежат рядом с файлами.

**Чего гейт A3 НЕ доказывает.** Совпадения гарнитур: шрифт опознаётся ролью, а не именем
(решение A4), поэтому наш текст шире оригинального там, где у образца узкая гарнитура.
Вырез товара в фазе A не готов — на образце «футболка» слой `cutout` снят правилом K-3, и
абзац, который в оригинале уходит за модель, лежит открытым. Оба расхождения ожидаемые и к
языку макета отношения не имеют.

- [x] **A8. Краска иконки — параметр сборки, а не свойство файла. Готово 2026-09-01** (решение
  пользователя). Иконка в базе — **общая**, она не принадлежит макету: один и тот же градусник
  ложится и под «температуру носки» куртки, и под «температуру хранения» БАДа. Значит цвет в
  файле — ошибка хранения: сейчас обе иконки нарисованы в `#1c1c1c` и на тёмной или оранжевой
  плашке утонут, а плашки всех трёх видов в библиотеке уже есть.

  **Правило рисования: одна краска на иконку.** И `stroke`, и `fill` — один цвет, в исходнике
  записанный как `currentColor`. Две краски удвоили бы подбор по контрасту, а выигрыша не
  дают: обе существующие иконки под правило уже подходят.

  **Краску задаёт макет, а не вычисляет сборщик.** По ADR-0012 сборщик детерминирован и
  художественных решений не принимает. Чтобы выбрать цвет по контрасту, ему пришлось бы знать,
  что под иконкой — плашка, градиент или фотография, — то есть растеризовать и сэмплировать
  посреди отрисовки. Арт-директор (B5) знает это бесплатно: он сам положил плашку. Поэтому у
  слоя появляется поле краски, как у текста давно есть `color`.

  **Проверка контраста — там, где фон известен.** Иконка в группе поверх фигуры со сплошной
  заливкой сверяется с ней по WCAG (порог 3:1 — норма для нетекстовой графики, не 4.5:1).
  Иконка прямо на фотографии статически не проверяется никем, и делать вид, что проверяется,
  — хуже, чем не проверять: читаемость там держится плашкой или скримом, и это решение макета.

  Сделано: поле `ink` у `AssetLayer` · подстановка `currentColor` в сборщике · проверка
  контраста в валидаторе · обе существующие иконки перерисованы на `currentColor`.

  **Попутно закрыт дефект, который до этого молчал.** Три разбора из 31 положили в `content`
  словесные описания сцены вместо данных — строки в `frames` и `logo`, хотя эти поля из
  разбора приходить не должны вовсе: кадр и логотип подставляются на сборке. Сборщик получал
  строку там, где ждал картинку, и рисовал `href="undefined"` — невидимую битую картинку без
  единого сообщения. Подстановка краски превратила это молчание в падение, дефект стал виден
  и закрыт: проверка содержимого в `parse.mts` теперь отвергает `frames`, `cutout` и `logo` в
  разборе и сверяет форму образцов цвета, а три файла вычищены.

### Фаза B — пользовательский конвейер

**Развилки закрыты 2026-09-01 решениями пользователя.** Дальше фаза исполняется в Harvi Code
по холодному наряду [`m7-phase-b-handoff_2026-09-01.md`](m7-phase-b-handoff_2026-09-01.md).

| Развилка | Решение | Где записано |
| --- | --- | --- |
| Где живёт библиотека макетов | Таблица в базе рядом с иконками и шрифтами; файлы разборов — рабочая копия | [ADR-0013](../../docs/adr/0013-layout-library-in-database.md) |
| Что помнит генерация | Снимок макета + содержимое + карта шрифтов, а не ссылка на библиотеку | ADR-0013 |
| Как подбирается макет | Жёсткий фильтр по категории и пропорции, дальше скоринг; ничья — детерминированно; пусто — универсальный макет | шаг B2 ниже |
| Откуда теги | Батч-проход модели по образцам, подтверждение пользователем в витрине V-12 | шаг B2.0 ниже |
| Где крутится растеризатор | Edge Function; байты `resvg.wasm` и файлы шрифтов — из приватного бакета, один раз на холодный старт | шаг B0.1 ниже |
| Граница фазы | Целиком, включая B4 и B5 | решение пользователя 2026-09-01 |
| Чем режется товар | Своя ONNX-модель за интерфейсом `cutout-runner`; место запуска выбирает бесплатный замер | [ADR-0014](../../docs/adr/0014-cutout-runner-onnx-behind-interface.md) |

**Порядок исполнения:** B0 → B0.1 → B2.0 → B1 → B2 → B3 → B6 → B4.0 → B4 → B5 → B7.
Номера B1–B7 сохранены: на них ссылаются архивные заметки и коррекции K-1…K-4.

**Ревью Opus 2026-09-02 (шаги B0–B2, исполнены в Harvi Code).** Вердикт — принять с правками;
правки внесены в той же ветке. Что исправлено: подбор больше не отдаёт универсальный макет
из-за нулевого счёта — макет, прошедший фильтр по категории и кадру, лучше универсального,
у которого пропорция вообще не проверяется · универсальный макет в библиотеке теперь ровно
один (уникальный частичный индекс) и читается упорядоченным запросом · список свойств в
мастере получил стабильный ключ строки вместо индекса массива: после удаления строки фокус
оставался в поле, ставшем чужим · из входа подбора убрано неиспользуемое поле `wishes`.
Прогнано: `npm test`, `npm run lint`, `npm run build`. Долг по `npm run test:db` закрыт
2026-09-02 вместе с шагом B3: `supabase db reset` накатывает все миграции с нуля начисто,
135 проверок базы зелёные. Первый прогон падал на двух файлах — локальная база отставала на
миграцию `20260901170000` (единственный универсальный макет), в самих миграциях дефекта не
было.

- [x] **B0. Схема библиотеки и переезд разборов.** Миграция: `card_layouts` (макет `jsonb`,
  теги внешними ключами на `categories` / `marketplaces` / `presets`, признак логотипа,
  признак «кисти скрыты» из K-4, `is_fallback`) и `generation_cards` (снимок макета,
  содержимое, карта шрифтов). Производные признаки — ёмкость модулей, число кадров, наличие
  `cutout`, пропорция — **вычисляются из макета**, а не проставляются. Команды `push` / `pull`
  в `tools/card-pipeline` — в том же изменении, иначе рабочая копия и база разъедутся сразу.
  Тест базы: универсальный макет в библиотеке есть. Витрина [V-12](../../docs/VISUALS.md#v-12)
  переиздаётся по данным базы.
- [x] **B0.1. Рантайм сборки — Edge Function.** `npm:@resvg/resvg-wasm@2.6.2`; байты wasm и
  файлы шрифтов лежат в приватном бакете и тянутся готовым `downloadFile()` **один раз на
  холодный старт**, дальше живут в модульном кэше. Во время рендера сеть не трогается —
  правило ADR-0012 держится. Замер фазы A даёт ориентир: загрузка wasm 2,4 МБ — 2616 мс,
  `initWasm` — 7 мс, кадр 896 px — 83 мс. Клиент остаётся запасным путём без второй
  реализации. **Не проверено в изоляте (ревью 2026-09-02):** `renderCard` пока никто не
  вызывает, а числа замера сняты в Node на фазе A. Разовый смоук в локальном рантайме функций
  нужен до B7 — иначе отказ `resvg-wasm` под лимитами Edge вскроется на самом дорогом к
  переделке шаге.
- [x] **B2.0. Теги для 34 разборов.** Батч-проход модели по образцам (та же машинерия, что у
  `parse.mts`: офлайн, вдвое дешевле, ответа никто не ждёт) предлагает категорию, сценарий,
  площадку и признак «кисти скрыты». Словарь закрытый — значения обязаны существовать в
  справочниках. Предложения визуально сверены с 34 сохранёнными образцами и подтверждены
  пользователем в V-12: 31 стартовый образец отмечен Wildberries, у трёх референсов площадка
  не указана; у 19 макетов кисти скрыты, у 15 видны. Несогласия исправлены строками в базе,
  без перезапуска прохода.
- [x] **B1. Свойства товара.** Дешёвая текстовая модель извлекает свойства из описания и
  пожеланий, ранжирует по важности. Интерфейс под полем описания: изменить порядок, удалить,
  дописать. Придумывать свойства, которых нет во вводных, запрещено промптом — за них
  продавец отвечает перед площадкой. Реализовано 2026-09-01: список подтверждается и хранится
  с заявкой в продавцовом порядке; старые черновики совместимы, а устаревший ответ модели не
  перезаписывает ручную правку.
- [x] **B2. Подбор макета.** Вход — категория, площадка, сценарий, пожелания, наличие
  логотипа, число свойств из B1. Жёсткий фильтр: категория и пропорция кадра. Дальше баллы:
  совпал сценарий +3 · ёмкость модулей не меньше числа свойств +2 · логотип есть и макет его
  использует +1 · совпала площадка +1. Ничья разрешается детерминированно (хеш `generationId`),
  чтобы одинаковый ввод не прыгал между попытками. Ничего не нашлось — универсальный макет;
  генерация не падает никогда. Полки, а не векторный поиск: на пилоте это `where` по тегам,
  место под смысловой поиск в схеме остаётся.
- [x] **B3. Логотип.** Поле загрузки PNG со строгой проверкой (формат, прозрачность,
  минимальный размер). Не загрузил — слой снимается валидатором A1, пользователь предупреждён
  до запуска, генерация не блокируется.

  Сделано 2026-09-02. Проверка — одним файлом `supabase/functions/_shared/logo.ts` для обеих
  сторон: клиент отвечает человеку до отправки, `generate` перепроверяет те же байты до
  списания баллов. **Минимальная сторона снята с библиотеки, а не выбрана на глаз:** 11 гнёзд
  под логотип занимают 0,168…0,402 доли кадра, на самом крупном профиле (1792 px) это
  300…720 px — отсюда 400 px по длинной стороне. По длинной, а не по каждой: знак-надпись
  бывает 900×110.

  **Прозрачность проверяется по заголовку PNG, а не по пикселям** — альфа-канал в IHDR плюс
  чанк `tRNS`. Убедиться, что прозрачные пиксели там есть, значило бы распаковывать IDAT, а
  господствующий случай («сохранил знак на белом фоне») альфа-канала не имеет вовсе и
  отсекается заголовком. Остаток честно назван в шапке модуля, а не выдан за полную проверку.

  **Знак хранится отдельной колонкой `generations.logo_path`, а не пятым путём в
  `source_paths`.** Оба лежат в бакете `uploads`, но фото уезжают вендору референсами товара
  и возвращаются в мастер при повторе (US-E4), а знак не делает ни того ни другого. Схема
  держит и второе правило: знак допустим только у карточки — у генерации «фото» макета нет.
  Модерацию знак проходит в том же вызове, что и фото: он попадает в оплаченный кадр наравне
  с ними, а отдельный вызов стоил бы вторых денег за ту же проверку.

  Подбор макета получил настоящий `hasLogo` вместо заглушки `false` из B2.
- [ ] **B6. Бесплатное превью до оплаты (K-1).** Сборщик рисует макет на заглушке, показывает
  переполнения арифметикой, пользователь правит свойства. Платный вызов — после.
  **Плюс: превью обязано показывать отсечённые свойства** (решение по O-5 от 2026-09-01).
  Потолок модулей задаёт ёмкость выбранного макета, и список из B1 обрезается с хвоста — а
  значит молчаливое исчезновение свойства это ровно тот брак, который K-1 и ловит: продавец
  узнал бы о пропаже характеристики уже на оплаченном кадре. Показывать надо три вещи: что
  не поместилось, что причина — ёмкость макета (столько-то модулей), и что порядок правится
  здесь же. Отбрасывать по-прежнему с хвоста, но выбор, чему быть в хвосте, остаётся за
  пользователем.
- [ ] **B4.0. Замер раннера выреза — бесплатный, гейт перед B4.** Прогнать ONNX-модель через
  wasm в локальном рантайме функций на реальном кадре: память, время, качество кромки. Сверить
  с лимитами Supabase. Тот же замер в браузере. Отдельно — лицензии кандидатов **по
  первоисточникам**: у части моделей этого класса код открыт, а веса некоммерческие. Результат
  — таблица с числами и ссылками; место первой реализации выбирается по ней.
- [ ] **B4. Вырезание товара.** Из **одной** сгенерированной сцены (K-2), за интерфейсом
  `cutout-runner` — «кадр → маска того же размера» ([ADR-0014](../../docs/adr/0014-cutout-runner-onnx-behind-interface.md)).
  Маски нет — слой `cutout` снимается правилом K-3, карточка собирается без него.
- [ ] **B5. Арт-директор.** На вход: макет-образец, свойства из B1, тексты, **карта занятости
  кадра** после B4. На выход — валидный макет по схеме A1. Порядок именно такой: без карты
  кадра перекрытие текста товаром получается случайным.
- [ ] **B7. Сборка, выдача, пересборка.** Файл под профиль площадки; «переписать текст и
  пересобрать» — бесплатно, без вызова вендора; кнопка «Редактировать» присутствует и
  неактивна. Пересборка читает **снимок** макета из `generation_cards`, а не библиотеку.
  **Плюс смена шрифта после генерации** (просьба пользователя 2026-08-31, оценена как
  дешёвая). Шрифт хранится в макете **ролью**, а гарнитуру роли подставляет база шрифтов в
  момент сборки, — значит смена шрифта это другая карта «роль → гарнитура» и повторная
  сборка, без вызова вендора, ровно как правка текста. Стоит не механизма, а интерфейса:
  выбор из того, что есть в базе, и хранение выбранной карты рядом с генерацией. Одна
  оговорка: гарнитуры разной ширины, поэтому арифметика переполнения из B6 обязана
  пересчитываться на каждой пересборке, иначе строка молча вылезет за бокс.
  Здесь же гасятся два долга ADR-0012 — раньше их закрывать нельзя (решение пользователя
  2026-09-02), потому что каждый до сборки даёт регресс, а не пользу:
  - [ ] **Убрать вёрстку текста из промпта** — `cardLayoutLine` и `cardReferenceParts` в
    `supabase/functions/_shared/ai-provider/aitunnel.ts`. Тем же изменением, каким наш слой
    текста впервые ложится на кадр: раньше — карточка возвращается без текста вовсе, позже —
    текст в кадре дважды.
  - [ ] **Вернуть `describeProfileMismatch` к точному совпадению** —
    `supabase/functions/_shared/output-profile.ts`, порог ослаблен миграцией `20260829140000`.
    Несовпадение роняет генерацию, а размер задаём мы только после сборки, — значит правка
    едет ровно с B7.

### Фаза C — замеры и наполнение

- [ ] **C1. Механическая приёмка.** Дословность текста, отсутствие переполнений,
  соответствие профилю площадки — всё проверяется программно, на каждой сборке. Бесплатно.
- [ ] **C2. Замер кадров с человеком.** ⚠️ платный. Частота дефектов анатомии на
  `gpt-image-2`, отдельно по макетам с открытыми и скрытыми кистями (K-4). Разрешение на
  платный прогон спросить; логи вендора включить **до** запуска.
- [ ] **C3. Полный набор образцов** по решению вопроса O-2 и результатам пилота.
- [ ] **C4. Приёмка вслепую.** Наши карточки вперемешку с образцами, вердикт пользователя.
  Порог — его решение (решение 13 отложило критерий, но не отменило проверку).

## Открытые вопросы

### Закрытые 2026-08-31

- **O-1. Порядок вех — закрыт.** M7 идёт **перед** M6, наполнение сценариями показа переехало
  в этот план. Дорожная карта обновлена: [`00_GENERAL_PLAN.md`](../reference/00_GENERAL_PLAN.md),
  разделы «Дорожная карта», «Покрытие требований вехами», «Ближайший шаг». Вехи не
  переименовывались — на «M6» ссылаются архивные планы.
- **O-2 и O-3 — закрыты одним решением.** Стартовый набор: **по одному образцу на каждую пару
  «категория × маркетплейс»**, 21 штука, отбор за пользователем. Площадка остаётся **тегом
  образца, а не осью библиотеки** — при одном образце на ячейку это одно и то же по составу,
  но растёт потом без пересборки базы. Приоритет явно расставлен пользователем: **сначала
  работающий конвейер (образец → иконки → шрифты → пополнение), потом объём и разнообразие.**

### Открытые

- **O-4 закрыт по направлению 2026-09-01.** Вырез считает **своя** ONNX-модель за интерфейсом
  `cutout-runner`, платный сервис не берётся — [ADR-0014](../../docs/adr/0014-cutout-runner-onnx-behind-interface.md).
  Попутно снят вопрос пользователя «а разве модель сама не умеет»: умеет, но оба доступных
  через неё пути (товар на прозрачном фоне отдельной генерацией · редактирование сцены)
  возвращают **новые пиксели**, а слою `cutout` нужен тот же растр — иначе двойной контур.
  Остаток вопроса — не развилка, а замер: какая именно модель и где она крутится, решает
  бесплатный шаг B4.0.
- **O-5 закрыт пользователем 2026-09-01.** Отдельного числа не вводим: **потолок задаёт
  ёмкость выбранного макета** (число привязок `prop`), а список свойств из B1 ранжирован по
  важности, поэтому лишнее отсекается с хвоста. Довод — макет уже знает, сколько модулей в
  нём помещается, и второе число рядом с ним разъехалось бы с первым. Возражение «пользователь
  не увидит, почему свойство не попало в кадр» снято не отменой правила, а требованием к
  шагу B6: превью показывает отсечённые свойства и причину. Формулировка — в B6 выше.
- **O-6. Кнопка «Редактировать».** Ведёт в стороннее приложение. Искать кандидата уже сейчас
  (лицензия, интеграция) или зафиксировать только точку подключения?
- **O-7 закрыт пользователем в тот же день, не открываясь.** Вопрос был поставлен шире, чем
  нужно: из образца мы делаем **макет**, а не обязательство по содержимому. Макету достаточно
  объявить возможность — «в этой плашке может быть картинка, может быть текст, может быть и
  то и другое», — а откуда возьмётся конкретная картинка, макет не решает и решать не должен.
  Никакого нового слова в словаре это не потребовало: плашка — группа без привязок, картинка
  и текст — её слои с привязками.
  **Но проверка вскрыла настоящую дырку рядом.** Правило «пустая группа снимается» считало
  нарисованные слои, а не получившие содержимое, — и плашка, будучи слоем декоративным,
  держала группу живой сама за себя. У продавца без знака в кадре осталась бы пустая белая
  коробка. Исправлено: группа снимается, если у неё есть привязанные слои и **ни один** не
  получил содержимого; группа вовсе без привязок — украшение, снимать её не за что. Три
  теста на три исхода (картинка и текст · только картинка · ничего).
  Остаток, ждавший шага B3, закрыт вместе с ним 2026-09-02: **знак продавца теперь
  загружается** — поле на шаге «Как показать», хранение в `generations.logo_path`, привязка
  `logo` наполняется им на сборке. Картинка в плашке по-прежнему приходит из базы иконок;
  понадобится принимать бренд-ассеты продавца сверх логотипа — это расширение загрузки, а не
  языка.

## Verification

- **A3 пройден:** собранная по разбору карточка визуально сопоставима с оригиналом на трёх
  образцах. Это гейт: без него фаза B не начинается.
- **Текст детерминирован:** на 20 сборках подряд заголовок и описание совпадают с заданными
  **побайтово**. Это проверяется программно и заменяет вероятностный критерий старой схемы.
- **Профиль площадки:** каждый выходной файл проходит `describeProfileMismatch` без замечаний
  при **точном** попадании в целевой кадр, а не только в порог.
- **Отсутствующий ассет не ломает макет:** сборка без логотипа и без иконки даёт корректную
  композицию (проверка правила K-3).
- **Пересборка бесплатна:** правка текста и повторная выдача не создают вызова вендора и не
  меняют баланс баллов.
- **Главный сценарий US-01** проходит целиком на живом вендоре с пилотной библиотекой.
- **FR-07** выполняется на всех сборках; **FR-25** — точным размером, а не порогом.
