# Веха M5 — Реальный провайдер

Status: ACTIVE (с 2026-08-29)

## Context

M1–M4 закрыты и заархивированы. Сценарий US-01 проходится целиком **на заглушке**: мастер из
шести шагов, `generation-service` + `generation-worker`, асинхронный статус, переживающий F5,
результаты, скачивание и каталог. Интерфейс `ai-provider` из четырёх независимых операций
закрыт ([`supabase/functions/_shared/ai-provider/types.ts`](../../supabase/functions/_shared/ai-provider/types.ts)),
профиль вендора вынесен в конфигурацию — ни базового URL, ни имён моделей в коде нет.
Выбор реализации в [`index.ts`](../../supabase/functions/_shared/ai-provider/index.ts) при
неизвестном имени **падает, а не откатывается на заглушку**: незамеченная опечатка в
`AI_PROVIDER` на проде означала бы, что пользователи платят баллами за плейсхолдеры.

Теперь у выбора вендора появилось на чём проверяться. Это главный риск продукта
([`TZ.md`](../../docs/TZ.md) §10): **если товар на выходе не похож на загруженный, продукт не
нужен** — ни цена, ни скорость этого не компенсируют. Поэтому веха начинается не с интеграции,
а с приёмочного стенда: контрольная выборка реальных фото и сравнение кандидатов по одному
критерию — узнаваемости предмета.

Второе, что делает эта веха: **себестоимость перестаёт быть догадкой.** Юнит-экономика
([`UNIT_ECONOMICS.md`](../reference/UNIT_ECONOMICS.md)) считалась по коммерческому прайсу
Google как по ориентиру, вендор при этом не выбран ([ADR-0005](../../docs/adr/0005-ai-provider-abstraction.md)).
Пока фактического расхода на `generationId` нет — в миграциях нет ни одного поля
себестоимости — утверждение «прайс в баллах не убыточен» ничем не подкреплено.

Третье: **окружений становится три.** До сих пор их было два — local с заглушкой и prod
([`SPEC.md`](../../docs/SPEC.md) §6); staging был осознанно отложен именно до этой вехи
(генплан, таблица решений). Боевого вендора нельзя отлаживать на проде, а на local его
включать — жечь бюджет каждым прогоном.

## Цель

За интерфейсом `ai-provider` стоит выбранный по делу вендор: на контрольной выборке фото
товар на выходе узнаваем, категория всегда принадлежит перечню [`TZ.md`](../../docs/TZ.md) §5.1,
названия генераций осмысленны. Себестоимость каждого вызова известна и лежит рядом с
`generationId`, а прайс в баллах сверен с ней по факту, а не по прайсу третьей стороны.
Отказ вендора отрабатывает как US-E4 — полным возвратом — на живом API, а не только в тесте.

## Границы

**Входит:** контрольная выборка фото и приёмочный стенд сравнения кандидатов · выбор вендора
и **ADR на выбор** · реализация всех операций за интерфейсом — пяти, с `moderate` (FR-03, FR-04, FR-07,
FR-16) · учёт себестоимости и длительности рядом с `generationId` ([`SPEC.md`](../../docs/SPEC.md) §8)
и сверка с ценой в баллах · NFR-01 (распознавание ≤ 5 с либо честный индикатор) · закрытие
`recognize` от злоупотребления · модерация загружаемого контента и срок хранения файлов ·
staging как третье окружение с лимитом расхода · US-E2 и US-E4 на живом вендоре.

**Не входит:** формулировки 24 сценариев показа — это M6, отдельная работа с контентом; здесь
остаются временные · аудит WCAG и локали (M6) · наблюдаемость целиком (M6; здесь только
события провайдера) · изменение самого интерфейса `ai-provider` — если под вендора он не
натягивается, это находка вехи и предмет отдельного решения, а не тихая правка контракта ·
эквайринг, видео, соцвход — вынесены за scope версии ([`TZ.md`](../../docs/TZ.md) §3).

**Спорное — референсные карточки в системном промпте.** Лежат в
[`docs/assets/cardsforsysprompt/`](../../docs/assets/cardsforsysprompt/), в генплане записаны
в «Дальнейшие апгрейды». Веху не блокируют (промпт — данные, правится без релиза), но именно
здесь впервые появляется чем проверить эффект. Решение — шаг 2, по результату сравнения:
если узнаваемость проходит и без них, не тащим.

## Шаги

### 0. Развилки, которые решаются до кода

- [x] Ветка `claude/m5-real-provider` от `main` (доверенная полоса, Opus,
      [ADR-0002](../../docs/adr/0002-trust-lane-by-model-not-host.md)).

  > **Расхождение с планом, 2026-08-29.** `main` оказался на 6 коммитов позади и **не
  > содержит вехи M4**: ветка `claude/m4-generation-stub` не слита. Ветвиться от `main`
  > буквально значило бы начинать M5 без кода, на котором она стоит. Ветка заведена от
  > вершины `claude/m4-generation-stub`; `main` перематывается на неё fast-forward, так что
  > после слияния M4 база M5 совпадёт с `main` задним числом. **Слияние M4 в `main` — решение
  > пользователя, не сделано.**
- [x] **Ответы на открытые вопросы вехи** ([`TZ.md`](../../docs/TZ.md) §11). Все три —
      продуктовые политики, а не техника, и додумывать их нельзя. **Решения пользователя
      2026-08-29:**

  > **Модерация загружаемого контента — силами вендора, дешёвой моделью `lite`, и стоит она
  > перед списанием баллов в `generate`.** Отдельного сервиса не заводим. Место выбрано не
  > из экономии вызовов, а потому что мимо `generate` не проходит ни одна генерация: повесь
  > модерацию только на распознавание — её обойдут, просто не нажав «распознать» и заполнив
  > поля руками. Цена — один дешёвый вызов и пара секунд на каждую заявку.
  >
  > **Следствие для контракта — подтверждено пользователем 2026-08-29.** У интерфейса
  > `ai-provider` было четыре операции; поставить модерацию в `generate` значит проверить
  > именно тот набор фото, что списывается, а не тот, что видел `recognize` раньше в
  > мастере (набор мог измениться). Добавлена **пятая операция**, `moderate(photos):
  > Promise<{ allowed: boolean; reason?: string }>` — независимая, по образцу ADR-0005;
  > четыре существующие не тронуты.
  >
  > **Сделано:** [`types.ts`](../../supabase/functions/_shared/ai-provider/types.ts) несёт
  > `moderate`, [`stub.ts`](../../supabase/functions/_shared/ai-provider/stub.ts) реализует
  > (крючок отказа — первый байт фото `0x00`, заголовка товара на этом этапе ещё нет, крючком-
  > префиксом не воспользоваться), [`generate/index.ts`](../../supabase/functions/generate/index.ts)
  > зовёт до `create_generation` и отвечает тем же по форме `409`, что и нехватка баллов
  > (US-E3) — тихо, без слова «модерация» пользователю. Три новые проверки в
  > `generation-flow.mjs`: отказ, баллы не списаны, заявка не создана вовсе. 33/33 живых
  > проверки зелёные, 76 pgTAP, 48 vitest, сборка и линтер чисты.

  > **Срок хранения — загруженные фото живут 3 дня, и мы прямо об этом говорим; результаты
  > хранятся бессрочно.** Более долгое хранение исходников уходит в «Дальнейшие апгрейды»
  > как часть будущей платной подписки. Три дня выбраны не как компромисс по Storage, а
  > потому что снимают конфликт с US-E4: повтор неуспешной генерации внутри срока работает
  > как задумано. FR-17 не страдает — из каталога скачивается результат, а он остаётся.

  > **Ожидаемые объёмы (NFR-11) — до 50 генераций в сутки на пике.** Пилот. Бесплатного
  > тарифа Supabase хватает, лимит расхода на staging ставится небольшим.

- [ ] **Доступ к кандидатам.** Сравнение — платные запросы к чужим API.

  > **Найдено 2026-08-29:** маршрут доступа уже зафиксирован в
  > [`SPEC.md`](../../docs/SPEC.md) §5 — **шлюз AITunnel**, OpenAI-совместимый HTTP,
  > база `https://api.aitunnel.ru/v1`, ключ заголовком. Юрисдикция и оплата из РФ этим
  > закрыты, и вопрос сужается с «какие вендоры допустимы» до «какие модели шлюза
  > сравниваем». Приятное следствие: кандидаты переключаются переменными
  > `AI_PROVIDER_IMAGE_MODEL` / `AI_PROVIDER_TEXT_MODEL` — стенд гоняет их **без единой
  > правки кода**, ровно как задумано в ADR-0005.

  > **Модели выбраны пользователем 2026-08-29 — обе из того же шлюза:**
  > изображения — **`gemini-3-1-flash-image`**; распознавание, модерация, тексты карточки и
  > название генерации — **вариант `lite`** как дешёвый. Точные строки идентификаторов
  > сверяются со списком моделей шлюза в момент прогона: в код они не попадают, их место —
  > `AI_PROVIDER_IMAGE_MODEL` и `AI_PROVIDER_TEXT_MODEL`.
  >
  > **Сверено с живым каталогом шлюза 2026-08-29** (`GET /public/aitunnel/models/images` и
  > `/models/chat`, без ключа — публичные, JSON прочитан напрямую, не пересказом
  > инструмента выборки). Три находки:
  > 1. **Точное имя — с точкой, не с дефисом:** `gemini-3.1-flash-image`, текстовый lite —
  >    `gemini-3.1-flash-lite` (`gemini-3.1-flash-lite-image` — это lite для картинок,
  >    другая модель; текст и распознавание идут через `gemini-3.1-flash-lite`). Неверная
  >    строка в переменной окружения — это не опечатка, замеченная в код-ревью, а вызов,
  >    падающий на проде.
  > 2. **Обе модели поддерживают референсные фото** (`supports_edit: true`,
  >    `max_input_references: 14` — с запасом против наших 4 по FR-02). Формат —
  >    `POST /v1/images/generations`, поле `input_references: [{ type: "image_url",
  >    image_url: { url: "data:image/jpeg;base64,…" } }]`. Без этого подтверждения был риск
  >    писать `generateImages` на не тот эндпоинт и тихо генерировать по одному промпту,
  >    без вашего фото — то есть проваливать критерий узнаваемости по вине клиента, не модели.
  > 3. **Каждый ответ на генерацию несёт `usage.cost_rub` и текущий баланс.** Себестоимость
  >    для шага 4 не нужно вычислять по прайсу — шлюз отдаёт её числом в самом ответе.
  >    Цена по каталогу (не итог реального вызова): `gemini-3.1-flash-image` 5,1–22,1 ₽ за
  >    изображение (зависит от разрешения), `gemini-3.1-flash-lite-image` — 2,89 ₽
  >    фиксированно. Единицы — **рубли**, не доллары (инструмент выборки страницы дал
  >    доллары — расхождение поймано сверкой с сырым JSON, а не принято на веру).
  >
  > **Найден и исправлен смежный баг**: [`.env.example`](../../.env.example) называл эти
  > переменные иначе (`AI_GATEWAY_BASE_URL`, `AI_IMAGE_MODEL`, `AI_TEXT_MODEL`) и не имел
  > переменной для ключа вовсе — рассинхрон с `index.ts` (заведён на M1 раньше, чем модуль
  > `ai-provider` написан на M4). Приведено к тем именам, что реально читает код, и добавлена
  > `AI_PROVIDER_API_KEY` (по образцу `SUPABASE_SERVICE_ROLE_KEY` — секрет читается прямо
  > внутри реализации провайдера, в `ProviderProfile` не идёт: тип не должен нести секреты).
  >
  > **Два следствия, которые нельзя потерять.**
  > 1. [`SPEC.md`](../../docs/SPEC.md) §3 хранит устаревшее решение — `gemini-2-5-flash-image`
  >    от 2026-08-27. Строка правится вместе с ADR-0006 на шаге 2, а не молча сейчас:
  >    решение и доказательство пишутся одним изменением.
  > 2. [`UNIT_ECONOMICS.md`](../reference/UNIT_ECONOMICS.md) считалась по прайсу Gemini 2.5
  >    Flash Image ($0,039 за изображение) и по текстовым ставкам $0,3/$2,5 за 1M. Обе базы
  >    сменились, и там же честно записано, что ставки шлюза не сверены. Значит утверждение
  >    «прайс в баллах не убыточен» подтверждается **фактом прогона на шаге 4**, а не
  >    переносится по инерции.
  >
  > **Решение пользователя 2026-08-29: первоначальный бюджет приёмочной проверки кандидата —
  > 500–600 ₽.** Это потолок/диапазон расходов только для первого прогона контрольной выборки,
  > а не разрешение отправлять платные запросы: до прогона нужны предоставленный ключ шлюза
  > (только в секретах Edge Functions, не в git) **и отдельное явное подтверждение пользователя**.
  > Фактические расходы каждого вызова войдут в сводку.
  >
  > TODO(пользователь): предоставить ключ шлюза и дать явную команду на оплачиваемый прогон
  > после утверждения контрольной выборки.

- [ ] **Нужна ли смена контракта под вендора** — решается после шага 2, не раньше. Правка
      четырёх операций ради одного кандидата обесценивает абстракцию: смысл деления в том,
      что кандидаты сравнимы, потому что контракт один.

### 1. Приёмочный стенд: контрольная выборка и критерий

Сравнивать «на глаз по одной картинке» — это не выбор, а впечатление. Стенд должен давать
один и тот же вход всем кандидатам и складывать выходы рядом.

- [x] **Контрольная выборка фото.** Реальные фото товаров, а не стоки: рынок ломается на
      честных пользовательских снимках — блики, фон комнаты, ракурс.

  > Каталог-кандидат [`bench/samples/control-photo-catalogue.html`](../../bench/samples/control-photo-catalogue.html)
  > заведён пустым, затем заполнен поиском по открытым CC-лицензиям (2026-08-29, по просьбе
  > пользователя — «дёрнуть из интернета своими силами, утвердить через картотеку»). Найдено
  > 7 из 6 категорий, **пользователь утвердил все семь 2026-08-29**: настоящие любительские
  > снимки, не стоковые — коврик у двери, отражение неба в очках, размытый бытовой фон.
  > Перенесены в `bench/samples/<id>/` с `sample.json`, происхождение (автор, лицензия
  > CC BY/BY-SA, ссылка на файл и страницу-источник) записано в том же файле полем `source`.
  >
  > Прогон на заглушке ([`stub-real-photos-20260829T124948`](../../bench/runs/)) подтвердил
  > обвязку на всех семи: заявка принимается, файл соответствует профилю пары (FR-25),
  > тексты карточки и название генерируются, NFR-01 в норме. `FR-03` (категория) не совпала
  > ни разу — **это ожидаемо**: заглушка определяет категорию по хешу размера файла, а не по
  > содержимому, и не могла совпасть с реальным фото по построению (`bench/README.md`,
  > «стенд обязан честно провалить заглушку»).
  >
  > **Решение пользователя 2026-08-29: выборка окончательна на семи наборах.** «Гаджеты и
  > техника» сознательно не покрыта — не забыто, а осознанно принято: несколько заходов
  > поиска давали только студийные фото брендов (противоречат самому критерию «не стоковое
  > фото») или постановочный лайфстайл-кадр. Наборов меньше, чем целевые 10–15 из шага 1, но
  > шесть категорий из семи закрыты живыми любительскими фото с нужными трудными случаями
  > (мелкий предмет, отражение, текст на упаковке, бытовой фон) — этого достаточно, чтобы
  > дать приёмочному прогону содержательный ответ.

- [x] Скрипт прогона выборки через реализацию `ai-provider` (одну, любую) с сохранением
      входа, выхода, длительности и стоимости в каталог результатов. Живёт рядом с кодом,
      не в проде.
- [x] **Критерий узнаваемости зафиксировать словами до прогона**, иначе он подгонится под
      понравившийся результат. Минимум: предмет тот же (не «похожий из той же категории»),
      сохранены форма, цвет, надписи и логотипы, число объектов не изменилось.
- [x] Проверить стенд на заглушке: он обязан честно **провалить** её выход — заглушка
      возвращает фиксированные картинки, и они не имеют отношения к входу.

### 2. Выбор вендора и ADR

> **Постановка изменилась 2026-08-29.** Шлюз и обе модели пользователь назвал (шаг 0), так
> что шаг перестал быть выбором из кандидатов и стал **проверкой назначенного**: проходит ли
> `gemini-3-1-flash-image` критерий узнаваемости на реальных фото. Сравнение с альтернативой
> — не программа работ, а запасной ход на случай провала; главный риск продукта (ТЗ §10) от
> этого никуда не делся, и подтверждать выбор всё равно надо прогоном, а не доверием.
> Два сравнения при этом остаются осмысленными и дешёвыми: референсные карточки в промпте
> (с ними и без) и точность распознавания у дешёвой модели — её стенд меряет **машинно**,
> совпадением категории с эталоном, без участия глаз.

- [x] Прогнать выборку через кандидатов. Операции независимы — распознавание и тексты может
      закрывать одна модель, изображения другая ([ADR-0005](../../docs/adr/0005-ai-provider-abstraction.md)),
      и сравнивать их надо порознь.
- [x] Свести результат: узнаваемость по каждому набору · соответствие профилю
      «маркетплейс × категория» (FR-25 — вендор обязан отдать нужное разрешение и фон) ·
      себестоимость вызова · длительность.

  > **Прогон на живом вендоре состоялся 2026-08-29** — ключ и явная команда получены от
  > пользователя, бюджет 500 ₽. **Израсходовано 309,92 ₽, остаток 190,08 ₽.** Счёт шлюза
  > пополнен ровно на 500 ₽, так что бюджет ограничен самим балансом.
  >
  > **Текстовая половина контракта — чистый проход, 7/7 по каждому пункту:** FR-03 категория
  > совпала с эталоном во всех семи наборах (заглушка не совпадала ни разу — стенд честно её
  > проваливал), ТЗ §5.1 перечень 7/7, NFR-01 распознавание ≤ 5 с 7/7 с запасом втрое
  > (максимум 1,9 с), модерация — ноль ложных отказов. Тексты карточек и названия генераций
  > осмысленны. Четыре текстовые операции на генерацию стоят **0,19 ₽**.
  >
  > **Узнаваемость — порог НЕ взят.** Правило шага 1: проходит кандидат, у которого узнаваемы
  > все наборы. Итог боевого прогона — 4 из 7:
  >
  > | Набор | Вердикт |
  > | --- | --- |
  > | `accessories-watch`, `beauty-cream`, `clothing-shoes`, `other-bobblehead` | узнаваемы |
  > | `accessories-sunglasses` | **провал п.1** — предмет подменён другой моделью очков; вендор сам подписал «Модель: Авиатор», тогда как на входе складные очки в пластиковой оправе. В более раннем вызове на бакете 2K тот же набор дал не подмену, а разрушенный силуэт — провал устойчив, форма провала плавает |
  > | `home-chair` | **провал п.3** — салатовый стал жёлтым, бордовый алым: модель прочла триколор как флаг и подогнала цвета под флаг («в стиле флага» в её же тексте). На бакете 2K тот же набор цвета сохранял |
  > | `food-pepsi` | **провал п.4** — со входа исчезла отдельная банка, осталась только коробка. Воспроизвелось в обоих прогонах |
  >
  > **Недетерминированность — самостоятельная находка.** Один и тот же набор на одном и том
  > же запросе даёт разный результат: `accessories-watch` в одном вызове отдал верное
  > «СДЕЛАНО В СССР», в другом — искажённое «СЕКЗАНО & СССР». Значит вердикт по одному
  > прогону — оценка снизу, и подбор промпта нельзя проверять единичным вызовом.
  >
  > **Себестоимость замерена по факту** (`usage.cost_rub` шлюза, сверено с падением баланса):
  > изображение 11,54 ₽ на бакете 1K и 17,25 ₽ на 2K, текстовые операции 0,19 ₽,
  > инфраструктура ≈ 0,15 ₽. Генерация целиком — **11,88 ₽ (1K) или 17,59 ₽ (2K)**, с
  > поправкой на 10% брака 13,20 ₽ и 19,54 ₽ против **утверждённых 4,13 ₽**
  > ([`UNIT_ECONOMICS.md`](../reference/UNIT_ECONOMICS.md) §8). Валовая маржа пакета «Про»
  > падает с утверждённых 88,7% до **64,5% (1K)** и 47,5% (2K); «Стандарт» — 78,0% и 67,4%.
  > В убыток не уходим ни на одном пакете. Расхождение с расчётом объясняется целиком
  > бакетом: §8 считалась по цене 1K-изображения, а профиль требовал 1600 px и выталкивал
  > в 2K. Пересмотр профиля (ниже) вернул большинство пар на 1K.
  >
  > Артефакты: `bench/runs/aitunnel-images-*` (изображения напрямую, до правки обвязки),
  > `bench/runs/aitunnel-live-*` (боевой путь через Edge Functions). Каталог в `.gitignore`.

- [x] **Профиль FR-25 переописан требованиями площадок** — решение пользователя 2026-08-29
      после разбора, реальные требования трёх площадок предоставлены им же.

  > **Почему пришлось.** Профиль требовал JPEG ровно 1200 × 1600 — размера, выбранного нами,
  > а не площадками (так и было записано в прежнем seed-е). Живой вендор его не отдаёт и не
  > отдаст: генерация принимает бакеты (512 / 1K / 2K / 4K) и соотношение сторон, формат
  > выбирает сама. Проверка равенством отвергала бы **каждую** генерацию — пользователю
  > возврат баллов, нам уплаченные вендору деньги за выброшенный кадр. Площадки же формулируют
  > требования порогом и рекомендацией и принимают несколько форматов.
  >
  > **Сделано** (миграция `20260829140000_output_profile_requirements.sql`): профиль несёт
  > порог площадки, целевой кадр под ближайший проходящий бакет, явное соотношение сторон,
  > список принятых форматов и предел веса. Правило сверки вынесено в общий
  > [`output-profile.ts`](../../supabase/functions/_shared/output-profile.ts) — одно на
  > провайдера и воркер, по образцу `pricing.ts`. Размер и формат читаются из самого файла
  > ([`image.ts`](../../supabase/functions/_shared/image.ts), PNG добавлен к JPEG; без
  > зависимостей — запись B9). `docs/TZ.md` §5.2 и справочник требований обновлены тем же
  > изменением, как требует их собственное правило.
  >
  > **Три бага, найденные этим прогоном и починенные здесь же:**
  > 1. **Соотношение сторон выводилось из пикселей.** 1792 × 2400 сокращается в 56 : 75 —
  >    шлюз отвечал `HTTP 400: не поддерживается соотношение сторон "56:75"`. То же число
  >    ушло бы и в интерфейс вместо «3 : 4». Соотношение теперь хранится явно.
  > 2. **Бакет `results` принимал только `image/jpeg`.** PNG отвергался Storage уже ПОСЛЕ
  >    оплаты вызова: изображение получено, профиль пройден, загрузка — HTTP 400. Так
  >    потеряно четыре генерации из семи в первом боевом прогоне.
  > 3. **Пара Ozon × «Аксессуары» не имела строки-исключения** и получала белый фон вместо
  >    серого `#F2F3F5`, хотя справочник требует серый для «одежды, обуви **и аксессуаров**».
  >    Расхождение существовало с M4.
  >
  > **Операционная находка для стенда:** рантайм Edge Functions **сохраняет секреты через
  > `supabase db reset`** — заглушка сама собой не возвращается. Тест потока
  > (`npm run test:generation`) обязан идти на заглушке: его синтетические фикстуры живой
  > вендор отвергает («Unable to process input image»). Переключение — полный `stop`/`start`.
  >
  > **Проверено после правки:** 33/33 живых проверок на заглушке (M1–M4 не задеты —
  > абстракция не протекла), 76 pgTAP, 48 vitest, сборка и линтер чисты; боевой прогон
  > четырёх ранее упавших наборов — **0 машинных провалов**, включая FR-25 на всех.

  > **Риск, найденный 2026-08-29 при написании клиента** (шаг 3,
  > [`aitunnel.ts`](../../supabase/functions/_shared/ai-provider/aitunnel.ts)). Каталог шлюза
  > даёт под `generateImages` только *бакеты* разрешения (`512`/`1K`/`2K`/`4K`) и соотношение
  > сторон — не произвольные пиксели, а профиль (FR-25) требует точный `width`×`height`
  > (1200×1600 или 1600×1600). `generation-worker` уже сверяет это равенством без допуска
  > (`readJpegSize`, вехa M4) — рефакторить эту проверку сейчас не в скоупе шага 3. Клиент
  > берёт бакет не меньше длинной стороны профиля и **бросает диагностическую ошибку**
  > с фактическими и ожидаемыми размерами, если не совпало, — вместо тихой подгонки. Первый
  > реальный вызов этого шага сразу покажет, совпадает ли бакет+аспект с нужными пикселями
  > 1-в-1; если нет — нужен отдельный разбор (донастройка параметров запроса вендору, либо
  > шаг ресэмплинга, которого в проекте пока нет) прежде чем шаг 2 можно закрыть.
- [x] Проверить эффект референсных карточек из
      [`docs/assets/cardsforsysprompt/`](../../docs/assets/cardsforsysprompt/): тем же
      прогоном с ними и без. Результат — в решение, а не в «потом посмотрим».

  > **Прогнано 2026-08-30, бюджет пользователя ~50–70 ₽ на 4 генерации (использован
  > остаток 190,08 ₽).** Два отдельных эксперимента поверх уже утверждённых семи.
  >
  > **Эксперимент А — `home-chair` без слова «триколор» в названии.** Гипотеза: слово в
  > наименовании само подтолкнуло модель к ассоциации с флагом. **Результат смешан, а не
  > опровергнут:** новое название («чёрно-жёлто-красными блоками») по ошибке назвало те же
  > канонические флажные цвета явным текстом вместо салатового/бордового с исходного фото —
  > эксперимент невольно усилил сигнал вместо того, чтобы его убрать. Вывод по этому прогону
  > один: если явно попросить точные флажные цвета, модель их и рисует — ожидаемо, не находка.
  > Чистый тест («не называть цвета словами вовсе» или «явно назвать настоящие салатовый и
  > бордовый») не поставлен, стоит отдельного маленького прогона, если понадобится.
  >
  > **Эксперимент B — референсные карточки как визуальный пример стиля** (`clothing-shoes`,
  > `beauty-cream`, `other-bobblehead`, включён `AI_PROVIDER_CARD_REFERENCES=true` —
  > `aitunnel.ts`, `config.toml`). **Эффект виден и положительный на 3 из 3:** `beauty-cream`
  > получил иконки у буллетов и разделители секций вместо голого списка; `other-bobblehead` —
  > скобочный заголовок-плашку и вертикальный разделитель перед описанием; `clothing-shoes` —
  > мягче, но чище типографику. Идентичность товара на всех трёх не пострадала (то же лицо
  > New Balance 476, та же туба крема, та же фигурка). Фактическая себестоимость по логам
  > шлюза: 40,48 ₽ на три изображения + 0,59 ₽ текстовых операций ≈ 41 ₽ — в пределах бакетных
  > цен шага 2, доплаты за референсы шлюз не берёт.
  >
  > **Решение по функции:** флаг оставлен выключенным по умолчанию (`AI_PROVIDER_CARD_REFERENCES`
  > не задан → пустой список, поведение не меняется) — включение по трём образцам не
  > заменяет полноценное сравнение по всей контрольной выборке, а бэклог B11 (брак текста,
  > водяной знак WB) эта проверка не закрывает: референсы дизайна не про корректность самого
  > текста. Годится как обоснованный кандидат на решение B11, не как готовое решение.
- [ ] **ADR-0006 на выбор провайдера**: критерий, кто сравнивался, на чём, что решило.
      Необратимое-к-откату решение — по правилу канона это ADR, а не строка в плане.
- [ ] Показать сводку пользователю **до** интеграции. Выбор вендора — деньги и юрисдикция,
      а не только качество.

### 3. Реализация пяти операций

Новый файл рядом со `stub.ts` + ветка в `createProvider`. Всё, что выше по стеку, не трогается —
если тронулось, это находка, а не рефакторинг «заодно». Операций пять, не четыре: `moderate`
добавлена решением шага 0 (2026-08-29).

- [x] `moderate(photos)` → отказ до списания в `generate` (решение шага 0). Интерфейс
      ([`types.ts`](../../supabase/functions/_shared/ai-provider/types.ts)), заглушка
      ([`stub.ts`](../../supabase/functions/_shared/ai-provider/stub.ts) — крючок: первый
      байт фото `0x00`) и вызов в
      [`generate/index.ts`](../../supabase/functions/generate/index.ts) готовы и покрыты
      тремя проверками в `generation-flow.mjs` (33/33 живых). Реализация на живом вендоре —
      готова вместе с остальными четырьмя, см. ниже.
- [x] `recognize(photos)` → категория из перечня §5.1 + наименование (FR-03, FR-04).
      **Категорию вне перечня приводить к NULL, а не пропускать наружу**: US-E2 (не смогли
      распознать) — штатный исход, а битый `categoryId` ломает мастер.
- [x] `generateImages(...)` → изображения строго по `OutputProfile`: кадр, разрешение,
      формат, фон (FR-25). Фон уходит **в запрос**, а не подкладывается постобработкой —
      решение M4. Выход, не совпавший с профилем, — неуспех, а не «сойдёт».
- [x] `composeCard(...)` → заголовок и описание карточки (FR-07).
- [x] `nameGeneration(...)` → название генерации для каталога (FR-16).
- [x] Ключи — только секретами Edge Functions, никогда `VITE_*`. Профиль (`AI_PROVIDER`,
      `AI_PROVIDER_BASE_URL`, модели) — переменными окружения, как заложено.
- [x] Таймаут и поведение при отказе на каждой операции. Провал `composeCard` при удавшемся
      `generateImages` — **полный возврат, результат не отдаётся** (правило M4, US-E4).

  > **Сделано 2026-08-29:**
  > [`aitunnel.ts`](../../supabase/functions/_shared/ai-provider/aitunnel.ts) — пять операций
  > против шлюза, OpenAI-совместимый HTTP (`chat/completions` для текстовых операций дешёвой
  > моделью, `images/generations` для картинок). Категории вынесены в общий
  > [`categories.ts`](../../supabase/functions/_shared/ai-provider/categories.ts) — заглушка
  > и реальный клиент теперь читают один и тот же перечень §5.1, а не два дублирующих (то же
  > происхождение бага, что и в `.env.example` на шаге 0). Ключ читается из
  > `AI_PROVIDER_API_KEY` на каждый вызов; таймауты — 20 с текстовым операциям, 60 с
  > изображению; любой отказ (сеть, не-JSON, пустой ответ, размер вне профиля) — `throw`,
  > что штатно приводит к полному возврату баллов в `generation-worker` (US-E4) без правки
  > самого воркера. Реализация **не вызывалась по-настоящему** — ключа нет, платный прогон
  > не разрешён (шаг 0). Проверено то, что можно бесплатно: полный набор тестов на заглушке
  > не сломался (33/33 живых, 76 pgTAP, 48 vitest, сборка и линтер чисты) — модуль
  > `ai-provider` по-прежнему грузится и работает по умолчанию на `stub`, новый файл ничего
  > не задел выше по стеку. Найденный при написании риск точного совпадения пикселей —
  > записан в шаг 2 (там же, где стенд проверяет FR-25).

### 4. Себестоимость рядом с generationId

- [ ] Миграция: расход и длительность на вызов, привязанные к `generationId`
      ([`SPEC.md`](../../docs/SPEC.md) §8). В миграциях сейчас нет ни одного такого поля —
      это новое хранилище, а не колонка в существующей таблице.
- [ ] Писать факт по каждой операции отдельно: пять операций (считая `moderate`) могут уйти
      разным вендорам, и одна суммарная цифра тогда неразбираема.
- [ ] **Сверка с ценой в баллах.** Взять фактическую себестоимость по прогону выборки и
      сравнить с прайсом [`UNIT_ECONOMICS.md`](../reference/UNIT_ECONOMICS.md). Расхождение
      не «поправить по-тихому»: цена в баллах — согласованная с заказчиком цифра, её
      пересмотр — отдельное решение пользователя.

### 5. NFR-01 и защита recognize

- [x] Распознавание укладывается в 5 с — либо честный индикатор прогресса. Замер на живом
      вендоре, не на заглушке.

  > **Замерено 2026-08-29 на живом вендоре: 7/7 наборов уложились**, максимум 1,9 с при
  > бюджете 5 с — запас втрое. Индикатор прогресса не понадобился. Генерация изображения
  > при этом идёт 15–25 с, но она асинхронная и NFR-01 не покрывает.
- [x] **`recognize` больше нельзя оставлять открытым.** На заглушке это был бесплатный
      вызов; на живом вендоре эндпоинт, отвечающий гостю без входа, — это наш платёж за
      чужой трафик. Закрыть: вход, либо лимит по адресу/сессии, либо перенос распознавания
      за порог регистрации. Выбор влияет на UX мастера (гость проходит шаги до «Запустить
      генерацию» — FR-12) — решать вместе с пользователем, а не молча.

  > **Решение пользователя 2026-08-29: распознавание остаётся.** «В ТЗ есть — значит надо
  > делать». `FR-03` и `FR-04` реализуются на этой вехе как требования текущей версии,
  > вынос в «Дальнейшие апгрейды» снят с рассмотрения. Разбор, на котором принято решение
  > (что именно в ТЗ, себестоимость 0,13 ₽ из 4,13 ₽, полный список правок при удалении),
  > отдан пользователю отдельным артефактом.
  >
  > Остаётся выбрать **способ защиты**. Пользователь предложил четвёртый вариант: сделать
  > само распознавание платным — списывать баллы сразу, без спроса, невозвратно даже при
  > последующей неудачной генерации. Разбор — ниже, решение ждёт.

### 6. Модерация и срок хранения

Пишется по ответам из шага 0. Без них шаг не начинается — это политика, а не реализация.

- [ ] Модерация загружаемого контента по выбранной политике; отказ — **до списания баллов**.
- [ ] Срок хранения `uploads` и `results`; согласовать с FR-17 (повторное скачивание из
      каталога без списания) — молчаливое протухание оплаченного результата недопустимо.

  > **Решение пользователя 2026-08-29: загруженные фото живут 3 дня, и мы прямо об этом
  > говорим.** Результаты — бессрочно. Более долгое хранение исходников уходит в
  > «Дальнейшие апгрейды» как часть будущей платной подписки.
  >
  > Решение принято поверх находки: «удалять сразу» столкнулось бы с уже реализованным
  > US-E4 — `restoreDraftFrom`
  > ([`src/features/generation/api.ts`](../../src/features/generation/api.ts)) выкачивает
  > исходные фото из `uploads`, чтобы вернуть мастер к параметрам неуспешной генерации.
  > Три дня этот конфликт снимают: повтор внутри срока работает как задумано.
  >
  > **Что из этого следует для реализации:**
  > - удаление по возрасту объекта, а не по завершению генерации;
  > - `restoreDraftFrom` перестаёт молча писать `logger.warn` при пропавшем фото: после
  >   трёх дней это **штатный** исход, и мастер обязан сказать «параметры восстановлены,
  >   фото загрузите заново», а не подсунуть пустую загрузку под кнопку «Запустить»;
  > - срок называется пользователю на экране загрузки — «прямо скажем» из решения;
  > - FR-17 не затронут: из каталога скачивается результат, а он хранится бессрочно.

### 7. Staging

- [ ] Отдельный проект Supabase; миграции и функции катятся тем же конвейером, что на прод.
- [ ] Боевой вендор **с лимитом расхода** — числом из ответа про объёмы.
- [ ] Обновить [`SPEC.md`](../../docs/SPEC.md) §6: окружений три, у каждого свой профиль
      провайдера. Снять из генплана пометку «Staging — отложен до M5».
- [ ] Проверить, что local по-прежнему идёт на заглушке по умолчанию: разработка не должна
      жечь бюджет.

### 8. Прогон сценариев на живом вендоре

- [ ] US-E2 — не распознали: мастер не блокируется, пользователь правит руками (FR-05).
- [ ] US-E4 — вендор недоступен/отказал: баланс равен балансу до запуска, в каталоге ничего
      готового не появилось. Проверять **реальным** отказом (сорванный ключ, таймаут), а не
      подменённым ответом.
- [ ] Частичный неуспех «изображение есть, текстов нет» — полный возврат.

## Verification

- На контрольной выборке товар на выходе узнаваем по критерию, зафиксированному в шаге 1
  **до** прогона. Приёмочная проверка глазами, результат — в ADR-0006.
- Определённая категория **всегда** принадлежит перечню [`TZ.md`](../../docs/TZ.md) §5.1;
  не смогли — NULL и US-E2, но не значение вне перечня.
- Названия генераций в каталоге осмысленны: список читается без открытия карточек.
- Сгенерированный файл соответствует параметрам пары «маркетплейс × категория» (FR-25) —
  тот же критерий, что на M4, но теперь на реальном выходе.
- Себестоимость каждой генерации есть в БД рядом с `generationId` и **не превышает** её цену
  в баллах; расхождение вынесено пользователю решением, а не поглощено.
- Распознавание ≤ 5 с либо индикатор прогресса, замеренные на живом вендоре (NFR-01).
- `recognize` не отвечает неограниченно анониму.
- US-E4 воспроизведён реальным отказом вендора: баланс восстановлен полностью, каталог чист.
- Staging развёрнут, лимит расхода выставлен, local остался на заглушке.
- Существующие тесты M1–M4 зелёные: смена реализации за интерфейсом не должна их трогать —
  если тронула, абстракция протекла, и это находка вехи.

## Открытые вопросы вехи

Сведены в шаге 0.

**Отвечено 2026-08-29:** модерация — силами вендора, в `generate` до списания · загруженные
фото живут 3 дня и мы прямо об этом говорим, результаты бессрочно · объёмы — до 50 генераций
в сутки на пике · шлюз AITunnel (найдено в [`SPEC.md`](../../docs/SPEC.md) §5) · модели:
`gemini-3-1-flash-image` для изображений и вариант `lite` для дешёвых операций ·
распознавание остаётся, `FR-03` и `FR-04` делаются на этой вехе · первоначальный бюджет
приёмочной проверки кандидата — cap/диапазон 500–600 ₽ (не разрешение на запросы без ключа
и отдельной команды пользователя).

**Отвечено также:** `recognize` закрыт лимитом бесплатных распознаваний в сутки (10 гостю,
100 вошедшему), не платой — разбор и код в шаге 5.

**Отвечено также:** контрольная выборка — пользователь утвердил 7 из 6 категорий 2026-08-29
и закрыл вопрос на этом числе («Гаджеты и техника» осознанно не покрыта); наборы лежат в
`bench/samples/<id>/`, прогон на заглушке подтвердил обвязку · пятая операция `moderate`
подтверждена и реализована в интерфейсе, заглушке и `generate` — код и тесты в шаге 3.

**Найдено при подготовке к шагу 3:** точные id моделей шлюза — с точкой, не с дефисом
(`gemini-3.1-flash-image`, текстовый lite — `gemini-3.1-flash-lite`), сверено по сырому
JSON публичного каталога AITunnel · референсные фото передаются `input_references` на
`POST /v1/images/generations`, до 14 штук · каждый ответ несёт `usage.cost_rub` — готовая
себестоимость для шага 4 · попутно исправлен `.env.example` (называл переменные не так, как
их читает `index.ts`, и не имел переменной для ключа).

**Отвечено 2026-08-29 прогоном на живом вендоре** (ключ и команда получены, израсходовано
309,92 ₽ из 500, остаток 190,08 ₽): профиль FR-25 переописан требованиями площадок вместо
точного размера, выбранного нами · себестоимость генерации замерена по факту — 11,88 ₽ на
бакете 1K против утверждённых 4,13 ₽, в убыток не уходим · NFR-01 закрыт замером 7/7 ·
текстовая половина контракта прошла начисто 7/7 по всем пунктам.

**Главный открытый вопрос вехи — узнаваемость: порог не взят, 4 из 7.** Провалы устойчивы
(`accessories-sunglasses` — подмена предмета, `home-chair` — сдвиг цвета под флаг,
`food-pepsi` — потеря объекта), и результат недетерминирован: один и тот же набор на одном и
том же запросе даёт разный выход. **ADR-0006 на выбор провайдера писать пока не на чем** —
решение требует либо доработки промпта с замером на повторах, либо сравнения с другой
моделью, и это следующая работа шага 2, а не формальность.

Шаг 3 сделан целиком. Шаг 5 закрыт (NFR-01 замерен, `recognize` закрыт лимитом). Шаг 4
разблокирован и наполовину обеспечен: `usage.cost_rub` шлюза даёт готовую себестоимость на
каждый вызов, осталась миграция и запись рядом с `generationId`. Шаг 6 разблокирован. Шаг 7
упирается в отдельный проект Supabase. Шаг 8 стартует после того, как узнаваемость возьмёт
порог.

**Развилка вынесена в бэклог:** [B11](../BACKLOG.md) — текстовый блок карточки рисует сам
вендор, и рисует его с браком (4 кадра из 8, включая невыполненный плейсхолдер промпта и
чужой водяной знак «WB» на карточке для Ozon).
