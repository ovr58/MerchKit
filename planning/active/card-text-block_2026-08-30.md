# Текстовый блок карточки: вендор или своя вёрстка

Status: ACTIVE (с 2026-08-30)

## Context

При `kind === 'card'` мы просим модель изображений нарисовать текстовый блок **внутри кадра**
(`imagePrompt`, ветка `kind === 'card'` в
`supabase/functions/_shared/ai-provider/aitunnel.ts`). На первом платном прогоне живого
вендора (`gemini-3.1-flash-image` через AITunnel, каталог
`bench/runs/aitunnel-images-20260829T153546/`) этот блок вышел бракованным **в 4 кадрах из
8**: дублированное слово, разрушенная глифом строка, задвоенная единица измерения и
невыполненный плейсхолдер промпта прямо на картинке. В контрольном вызове модель дорисовала
водяной знак **«WB»** на наборе `ozon × accessories`. Товар при этом узнаваем везде: ломается
не предмет, а вёрстка поверх него. Подробности — запись B11 в `planning/BACKLOG.md`.

**Что уточнено при заведении плана (2026-08-30):**

1. **Плейсхолдер модель выдумала сама.** Ни в одном `bench/samples/*/sample.json` нет
   квадратных скобок и шаблонных фраз — вход чистый. Значит «Высота: [указать точную
   высоту, e.g., ~10см]» не пересказ нашего текста, а кусок **чужого шаблона карточки**,
   вытащенный из обучающих данных.
2. **У плейсхолдера и водяного знака «WB» один корень.** Слова Wildberries в промпте нет;
   единственная зацепка — фраза «Это карточка маркетплейса». По ней модель достаёт не
   «текстовый блок вообще», а **целиком запомненную карточку конкретной площадки** — вместе
   с её брендингом и её незаполненными полями. Это переводит задачу из «поправить
   формулировку» в «решить, просить ли у модели изображений вёрстку в принципе».
3. **FR-07 не требует текста внутри кадра.** Формулировка ТЗ — «формирует одно изображение
   карточки, заголовок и описание товара», три части. Что заголовок нарисован поверх
   картинки — решение реализации M4, а не требование. Свою вёрстку разрешает и текущее ТЗ.
4. **Тексты у нас уже есть отдельно и чистые.** `composeCard` пишет их в
   `generations.card_title` / `card_description`, экран показывает их рядом с изображением
   ([Generation.tsx:250](../../src/screens/Generation.tsx#L250)). На живом вендоре операция
   отработала 7/7 без брака.
5. **Своей вёрстки в Edge Function сейчас нечем сделать.** `_shared/jpeg.ts` кодирует только
   изображения из одноцветных блоков 8×8 (вырожденное ДКП), декодера нет вовсе;
   `_shared/image.ts` читает заголовки, пикселей не трогает. Зависимостей в функциях нет
   намеренно (B9). Полноценный путь «декодировать кадр вендора → нарисовать текст →
   закодировать обратно» — это baseline-декодер JPEG + честный кодировщик с ДКП + растеризатор
   шрифта, всё вручную. Это меняет цену варианта «своя вёрстка» и обязано быть учтено в
   развилке.

**Ожидаемый исход:** решение развилки, опёртое на измеренную частоту брака, а не на
впечатление; решение записано ADR; карточка на выходе не требует перечитывания глазами
перед выкладкой на площадку.

## Цель

Убрать класс дефектов «брак в текстовом блоке карточки», выбрав между доработкой промпта и
собственной вёрсткой текста на основании замера частоты брака на повторах.

## Критичные файлы

| Файл | Что с ним |
| --- | --- |
| `bench/run.mjs` | правка: `--repeat N` — тот же набор N раз, чтобы мерить недетерминированный брак |
| `bench/README.md` | правка: раздел про замер брака вёрстки и таксономия дефектов |
| `supabase/functions/_shared/ai-provider/aitunnel.ts` | правка: `imagePrompt`, ветка `kind === 'card'` |
| `docs/adr/00NN-card-text-block-source.md` | создать: решение развилки |
| `docs/TZ.md` | возможная правка формулировки FR-07 по итогу |
| `planning/BACKLOG.md` | пометить строку B11 «→ заведён план» |
| `planning/INDEX.md` | строка этого плана в `## active/` |

## Шаги

- [x] **1. Таксономия брака и повторы на стенде.**
      Зафиксировать в `bench/README.md`, что считается браком текстового блока, **до** первого
      замера — иначе критерий подгонится под понравившийся результат (та же дисциплина, что у
      критерия узнаваемости). Категории: (A) искажение/дублирование слова, (B) невыполненный
      плейсхолдер шаблона, (C) чужой брендинг или водяной знак, (D) текст поверх самого товара,
      (E) блок отсутствует вовсе. Отдельно — что в критерий **не** входит (красота вёрстки,
      шрифт, композиция: это M6). Добавить в `bench/run.mjs` аргумент `--repeat N`: список
      наборов повторяется N раз, идентификатор прогона — `<sample>#<n>`, отчёт кладёт повторы
      одного набора рядом. Правка минимальная — расширение списка перед циклом, не переписывание
      цикла.
- [x] **2. Базовый замер на текущем промпте.** ⚠️ платный
      Прогон на живом вендоре: 3 набора × 5 повторов = 15 генераций (~175 ₽ по 11,65 ₽ за
      генерацию). Наборы — `accessories-watch` (на нём брак уже воспроизводился),
      `food-pepsi` (текст на упаковке + единицы измерения), `clothing-shoes` (бренд на товаре).
      Разметить по таксономии шага 1, записать частоту по категориям в этот план.
      **Предусловие (найдено 2026-08-30):** локальный стек молча сидел на **заглушке** —
      пробный прогон `cardtext-probe` дал `cost: null`, «Куртка-бомбер» на фото наручных
      часов и синтетический кадр из блоков вместо фотографии.
      **Причина — расхождение имён переменных.** В корневом `.env` ключ шлюза лежит под
      именем `AITUNNEL_API_KEY`, а код читает `AI_PROVIDER_API_KEY`
      ([aitunnel.ts:56](../../supabase/functions/_shared/ai-provider/aitunnel.ts#L56)) и
      `AI_PROVIDER` ([index.ts:38](../../supabase/functions/_shared/ai-provider/index.ts#L38));
      `config.toml` подставляет те же `AI_PROVIDER*`. Незаданное имя не даёт ошибки: `||`
      уводит на заглушку **молча**. Проверено по именам переменных (без значений) в
      `supabase/.temp/start-secrets/supabase_edge_runtime_MK/env/docker.env` — там есть
      `AITUNNEL_API_KEY` и нет ни одной из пяти `AI_PROVIDER*`. На стейдже имена правильные,
      поэтому живые прогоны M5 шли там и расхождение не всплывало.
      **Решение (пользователь, 2026-08-30):** приводим `.env` к именам кода, а не наоборот —
      вендоро-нейтральные имена требует ADR-0005 и `docs/SPEC.md` §5. Правит пользователь:
      агенту `.env` читать и менять не разрешено. Подстановка `env(...)` разрешается в момент
      `supabase start`, поэтому нужен перезапуск стека целиком, а не функций.
      Сумма подтверждена пользователем 2026-08-30: 3 набора × 5 повторов.
      **Снято 2026-08-30:** имена в `.env` приведены к коду, стек перезапущен целиком,
      рантайм видит все пять `AI_PROVIDER*`, профиль — `aitunnel` с
      `gemini-3.1-flash-image` и `gemini-3.1-flash-lite`. Пробный вызов не делался: проверка
      имён и значения профиля исключает молчаливую заглушку без единого платного запроса.
      **Не забыть после шага 4:** вернуть `AI_PROVIDER=stub` в `.env` — иначе
      `npm run test:generation` и обычная разработка начнут жечь бюджет вендора (решение
      шага 7 вехи M5).
- [x] **3. Дешёвая гипотеза: правка формулировки.**
      Две правки в `imagePrompt`, ветка `kind === 'card'`: (а) убрать слово «маркетплейс» как
      якорь к запомненным чужим карточкам, оставив описание нужного результата без отсылки к
      площадке; (б) явный запрет на подстановку любых полей, кроме переданных, и на любые
      логотипы и водяные знаки, которых нет на фото товара. Формулировать общо, как уже
      сделано с оговоркой про триколор (`aitunnel.ts`, комментарий про `home-chair`), а не
      затыкать конкретный случай.

      Черновик замены `layoutLine` (применять **после** базового замера, иначе базы не
      останется):

      > На изображении, помимо товара, нужен аккуратный текстовый блок: название
      > «‹title›» и краткое описание — рядом с товаром, не поверх него. Никакого другого
      > текста в кадре: ни дополнительных полей и характеристик, ни подписей в квадратных
      > скобках, ни шаблонных заготовок, которые надо заполнять. Никаких логотипов, водяных
      > знаков и названий магазинов, которых нет на фото товара. Весь текст блока — на
      > русском языке, даже если сам товар и надписи на упаковке англоязычные.

      Последняя фраза добавлена по итогам базового замера: класс H (3 кадра из 15) — не
      каприз вендора, а наш пробел. Язык текста в `imagePrompt` не задан вообще.

      Убрано намеренно: «Это карточка маркетплейса» и «оформленный как обложка карточки
      товара». Обе фразы — якоря к запомненным чужим карточкам, а не к нужному результату;
      по гипотезе из Context именно они тянут за собой и чужой брендинг, и незаполненные поля.

      **(в) Отдельная гипотеза, дешёвая и пока не проверенная:** мы просим «краткое описание»,
      не говоря какое — текст описания модель сочиняет сама, хотя у нас он уже есть.
      `composeCard` отдаёт готовые заголовок и описание, но в `generation-worker` вызывается
      **после** `generateImages`
      ([index.ts:232](../../supabase/functions/generation-worker/index.ts#L232)) и в промпт
      изображения попасть не успевает. Перестановка вызовов передала бы модели точный текст
      для отрисовки вместо задания «придумай». Побочно меняется и порядок трат: отказ текстов
      перестал бы стоить уже оплаченной генерации изображения. Проверять **после** (а) и (б):
      три правки в одном замере неразличимы.

      **Применено 2026-08-30 (а) и (б), (в) — нет.** `layoutLine` заменён черновиком выше;
      причина каждой фразы записана комментарием прямо над ним, чтобы следующий читатель не
      вернул «карточку маркетплейса» как безобидную формулировку. Гипотеза (в)
      (перестановка `composeCard` перед `generateImages`) намеренно оставлена на потом:
      её эффект неотличим от эффекта (а)+(б), если внести обе правки в один замер.
      `npm run lint` и `npm test` (52 теста) зелёные; ветка `kind === 'photo'` в диффе не
      затронута.
- [x] **4. Перезамер на той же выборке.** ⚠️ платный
      Те же 3 набора × 5 повторов (~175 ₽), сравнение с базой шага 2 по категориям.
      Правка промпта считается сработавшей, только если категории (B) и (C) ушли в ноль:
      плейсхолдер и чужой брендинг — не «качество вёрстки», а брак, с которым карточку не
      выложить.
- [x] **5. Развилка и ADR.** → [ADR-0011](../../docs/adr/0011-image-model-gpt-image-2-with-fallback.md)
      Свести три пути с ценой каждого и выбрать: **(I)** оставить вёрстку вендору с
      исправленным промптом, приняв остаточную частоту как риск типа «карточка»;
      **(II)** своя вёрстка в Edge Function — требует декодера и кодировщика JPEG вручную
      (см. Context п.5), объём несопоставим с остальным планом; **(III)** своя вёрстка в
      браузере через нативный Canvas — тексты уже в БД, зависимостей не нужно, но меняет
      место, где рождается итоговый файл (`downloadResult` в
      [api.ts:291](../../src/features/generation/api.ts#L291)) и обязана сохранить FR-15
      («скачанный файл соответствует показанному») и FR-25 (профиль площадки).
      Записать выбор в `docs/adr/`, включая отвергнутые варианты и цену каждого.
- [x] **6. Реализация выбранного и проверка прогоном.** ⚠️ платный · сделано 2026-08-31, 24,58 ₽
      Выбран путь I со сменой модели (ADR-0011). Подшаги, в этом порядке:
      1. **Потолок времени.** `IMAGE_TIMEOUT_MS = 60_000` при 31–52 с у новой модели плюс
         ~32 с накладных нашего стека (B13) пробивается гарантированно. Поднять по измеренному
         распределению — или закрыть B13 раньше смены. Смена без этого меняет брак текста на
         таймауты.
      2. **Форма запроса.** Ветвление в `aitunnel.ts`: `size` в пикселях для моделей с пустыми
         `supported_resolutions`, нынешние `resolution` + `aspect_ratio` — для остальных.
         Наружу интерфейса `ai-provider` не протекает.
      3. **Резервная модель.** Отдельная переменная окружения + повтор на отказе по контенту
         (HTTP 400 системы безопасности) + тест на то, что отказ ведёт к повтору, а не к падению.
      4. **Промпт.** В кадр — только заголовок; описание не диктуем (13 из 13 против 1 дефекта
         из 7 на длинном варианте).
      5. **Подтверждающий прогон через боевой конвейер**, а не пробник: пробник обходит ровно
         тот стек, где живёт B13. 2–3 набора, ~60 ₽.
      6. **Уборка:** вернуть `AI_PROVIDER=stub` в корневой `.env`, иначе `npm run test:generation`
         и обычная разработка жгут бюджет вендора.
- [ ] **7. Согласовать артефакты.**
      Формулировка FR-07 в `docs/TZ.md` — если по итогу продукт делает не то, что она
      обещает. Строка B11 в `planning/BACKLOG.md` → «заведён план». Строка плана в
      `planning/INDEX.md`.

## Базовый замер: результат (шаг 2, 2026-08-30)

Прогон `bench/runs/cardtext-base-20260830T111600/`, 3 набора × 5 повторов, живой вендор
`aitunnel` / `gemini-3.1-flash-image`. Разметка — глазами по 15 кадрам.

**Факт по деньгам: 233,19 ₽, а не обещанные ~175 ₽.** Цена генерации изображения не одна,
а зависит от бакета разрешения профиля: 11,55–11,70 ₽ на кадре 1024 × 1024 (`ozon × food`,
бакет 1K) и **17,26–17,42 ₽** на 1792 × 2400 (`ozon × accessories` и `ozon × clothing`,
бакет 2K). Цифра 11,65 ₽ из вехи M5 — это только дешёвый бакет. Перезамер шага 4 стоит
столько же, ≈233 ₽.

### Частота по предзафиксированным категориям

| Код | Что | Кадров | Где |
| --- | --- | --- | --- |
| A | искажение или дублирование слова | **2 / 15** | watch-r1 «текстуtурированный»; watch-r5 блок нарисован дважды, во второй копии «НАРУЧНЬЕ» |
| B | невыполненный плейсхолдер шаблона | **1 / 15** | pepsi-r3 «Упаковка: 12 банок (× 355 мл)» — подстановка оборвалась, множитель без числа |
| C | чужой брендинг или водяной знак | **0 / 15** | — |
| D | текст поверх товара | **0 / 15** | — |
| E | блока нет вовсе | **0 / 15** | — |

Кадров хотя бы с одним дефектом A–E: **3 из 15 (20 %)**.

**Категория C не воспроизвелась.** Водяной знак «WB» из вехи M5 — по-прежнему одно
наблюдение на всю историю. Утверждать по этим данным, что правки промпта его убрали, будет
нечестно: он не показался и **до** правок. Частоту такого редкого события выборкой из 15
кадров не измерить — это отдельный вопрос, а не результат этого замера.

### Три класса брака, которых в таксономии не было

Найдены на этом же прогоне. Записаны отдельно от A–E: таксономия фиксировалась до замера
намеренно, и подмешивать в неё новое задним числом означало бы потерять смысл этой
дисциплины.

| Код | Что | Кадров | Где |
| --- | --- | --- | --- |
| F | выдуманная конкретная характеристика | **1 / 15** | watch-r3 «Диаметр корпуса: ~40 мм» — числа не было ни в одном входе |
| G | служебная разметка или лейблы шаблона | **3 / 15** | pepsi-r1 «PRODUCT: / DESCRIPTION: / PACKSIZE:»; watch-r1 «Категория: Аксессуары»; pepsi-r3 звёздочки markdown вокруг строки |
| H | текст блока не на русском | **3 / 15** | pepsi-r1, pepsi-r2, pepsi-r5 — блок целиком по-английски (в pepsi-r3 английские вкрапления) |

Кадров хотя бы с одним дефектом A–H: **8 из 15 (53 %)**.

### Что из этого следует

1. **Брак распределён не по кадрам, а по товарам.** `clothing-shoes` — 0 из 5, чисто по всем
   восьми категориям. `accessories-watch` — 3 из 5. `food-pepsi` — 4 из 5. Мерить «частоту
   брака вообще» бессмысленно: она зависит от товара.
2. **H — самый частый класс, и у него есть причина в нашем коде.** Английский вылезает там,
   где англоязычен сам товар (Pepsi). А `imagePrompt` **нигде не задаёт язык текста**:
   промпт написан по-русски, но требования «блок на русском» в нём нет. Это не свойство
   вендора, это наш пробел — и правится одной строкой.
3. **F и G — та же болезнь, что B, только в лёгкой форме.** Модель тянет из обучающих данных
   структуру чужой карточки: где-то незаполненное поле, где-то лейблы полей, где-то
   выдуманное значение. Гипотеза Context про якорь «карточка маркетплейса» ими
   поддерживается.
4. **Правка (в) из шага 3 повышается в приоритете.** Три из восьми классов (B, F, G) — про
   то, что модель сама сочиняет содержимое текста. Передача готового текста из `composeCard`
   убирает у неё саму возможность сочинять.

## Перезамер после правки промпта (шаг 4, 2026-08-30)

Прогон `bench/runs/cardtext-fixed-20260830T115829/`, та же выборка 3 × 5, тот же вендор.
Фактическая цена — **146,65 ₽** вместо ожидаемых ≈233 ₽, и это плохая новость, а не экономия:
**пять попыток из пятнадцати не дошли до кадра**, за них вендору не заплачено.

### Сначала о провалах: пять из пятнадцати

| Что | Сколько | Из лога |
| --- | --- | --- |
| Таймаут вызова изображения (`IMAGE_TIMEOUT_MS` = 60 с) | 3 | `Генерация … не удалась: Signal timed out` |
| Ошибка протокола http2 к `api.aitunnel.ru` | 1 | `http2 error: stream error received` |
| Обрыв TLS на распознавании (заявка не создалась вовсе) | 1 | `peer closed connection without sending TLS close_notify` |

Баллы за провалы возвращаются полностью (`fail_generation`, одна транзакция со статусом),
продуктовой дыры здесь нет. Но время вызова изображения выросло почти вдвое:

| Прогон | Кадров | min | среднее | max |
| --- | --- | --- | --- | --- |
| база | 15 | 14,8 с | **20,8 с** | 29,2 с |
| после правки | 10 | 20,7 с | **37,1 с** | 55,4 с |

**Причину этими данными не разделить.** Гипотеза первая: промпт стал длиннее и жёстче, модель
дольше думает. Гипотеза вторая: шлюз в этот час лихорадило — ошибки http2 и TLS от промпта не
зависят вообще. Обе правдоподобны, и выбирать между ними по одному прогону нечестно.
Практическое следствие от причины не зависит: потолок в 60 с задевается, и это отдельная
находка — [B13](../BACKLOG.md).

### Частоты: до и после

Оценка глазами по 10 полученным кадрам (база — 15). Проценты сравнимы, абсолютные числа — нет.

| Код | Что | База | После | |
| --- | --- | --- | --- | --- |
| A | искажение или дублирование слова | 2 / 15 | **2 / 10** | без изменений |
| B | невыполненный плейсхолдер | 1 / 15 | **0 / 10** | база слишком редка, чтобы судить |
| C | чужой брендинг, водяной знак | 0 / 15 | **0 / 10** | не воспроизводился и до правки |
| D | текст поверх товара | 0 / 15 | 0 / 10 | |
| E | блока нет | 0 / 15 | 0 / 10 | |
| F | выдуманная характеристика | 1 / 15 | **2 / 10** | не ушло |
| G | служебные лейблы шаблона | 3 / 15 | **2 / 10** | **сменило природу, см. ниже** |
| H | текст не на русском | 3 / 15 | **0 / 10** | **ушло полностью** |

Кадров хотя бы с одним дефектом: **4 из 10 (40 %)** против 8 из 15 (53 %). Только A–E:
2 из 10 (20 %) против 3 из 15 (20 %).

Где именно: watch-r4 — A («ручной ручной завод») + F («мануфактурный калибр», «ручной завод»
во входе нет: там «Круглый корпус, светлый циферблат, кожаный ремешок») + G; pepsi-r3 — F
(«Оригинальная упаковка Pepsi Jazz (образца 2006 года)» — года во входе нет); pepsi-r4 — A
(«Упаковка содержип содержит 12 алюминиевых банок»); pepsi-r5 — G. Шесть кадров чистые.

### Главный вывод: H убрано, G мы создали заново сами

**H ушёл в ноль, и это прямое следствие правки.** Все три английских кадра базы были на
англоязычном товаре; после явного требования русского языка ни одного не осталось. Строка,
которой в промпте просто не было, стоила трёх кадров из пятнадцати.

**G остался, но это уже другой G.** В базе это были куски чужого шаблона: «PRODUCT: /
DESCRIPTION: / PACKSIZE:», «Категория: Аксессуары». После правки чужих шаблонов нет —
зато в watch-r4 и pepsi-r5 модель нарисовала **«Название:» и «Краткое описание:»**, то есть
буквально пересказала слова моего собственного промпта («нужен … текстовый блок: название
«…» и краткое описание») как подписи полей. Якорь к чужим карточкам убран успешно; на его
месте появился якорь к формулировке задания.

Лечится это не очередной оговоркой «не рисуй слова "название" и "описание"» — так и будет
расти список заплаток. Лечится гипотезой (в): передавать модели **готовый текст** из
`composeCard` вместо задания «придумай название и описание». Тогда в промпте не будет слов,
которые можно принять за лейблы, и одновременно исчезнет почва под F (выдумывание фактов) —
единственным классом, который правка не тронула вовсе (1 / 15 → 2 / 10).

### Что это значит для развилки

Критерий приёмки шага 4 («B и C в ноль») формально выполнен: обе категории пусты. Но опираться
на это нельзя: B встречалась 1 раз из 15, C — ни разу за оба прогона. Ноль на выборке в
10 кадров у события с базовой частотой 1/15 не отличим от везения. **Честный итог: правка
убрала один класс целиком (H), один класс переродила (G), одного не коснулась (F), по двум
весомым (B, C) данных недостаточно.**

## Гипотеза (в): диктуем готовый текст (2026-08-30)

Внесена после перезамера — по его же результату: чужой шаблон из промпта ушёл, но на его
месте модель стала рисовать слова **моего** промпта («Название:», «Краткое описание:»).
Пока в задании есть слова-роли, их можно принять за подписи полей.

**Что сделано.** `composeCard` вызывается в `generation-worker` **до** `generateImages`, его
результат передаётся в провайдер новым полем `card: CardTexts | null` и подставляется в
промпт готовыми строками: «Крупной строкой: «‹title›». Под ней, помельче: «‹description›».
Воспроизвести дословно…». Слов «название» и «описание» в задании больше нет вообще.
Операции остались независимыми (ADR-0005): изменился порядок вызовов, не контракт.
Побочно закрыт перекос трат — отказ текстов больше не стоит уже оплаченной генерации
изображения.

Ветка `card === null` сохранена: поле nullable по контракту, а рисовать карточку вовсе без
блока нельзя (FR-07). Там осталась прежняя формулировка.

**Проверено дымовым прогоном:** `composeCard` отработал первым (0,04 ₽, запись в
`generation_costs` раньше вызова изображения) — новый порядок работает. Дальше вызов
изображения не дошёл: первый раз сеть оборвала тело ответа, второй — таймаут 60 с.

## Замер гипотезы (в) отложен: вендор деградировал

Два дымовых прогона подряд (12:53 и 12:59) не дали ни одного кадра. Причина — не код: до
изображения дело доходило, `composeCard` отрабатывал.

| Окно | `moderate` | `composeCard` | `generateImages` |
| --- | --- | --- | --- |
| база (11:16–11:24) | 3,8 с | 2,4 с | **20,8 с** |
| после правки промпта (11:58–12:10) | 4,2 с | 1,0 с | **37,1 с** |
| сейчас (12:53–13:01) | **7,3 с** | 3,4 с | оба вызова провалились |

**Эта таблица разделяет две гипотезы из B13, которые раньше были неразличимы.** В окне
«после правки» текстовые операции шли ровно с той же скоростью, что и в базе — а их промпты
я не трогал. Замедлились **только** вызовы изображения, чей промпт стал длиннее и жёстче.

**Отменено в тот же день логом вендора (см. раздел ниже).** Вывод «длинный промпт заставляет
модель думать дольше» держался на том, что своей стороны провода мы не видели. Теперь видно:
вендор считает свои вызовы секундами, а мы — десятками секунд. Время уходит не в модель.

**Следствие для замера:** гонять 15 кадров в таком окне бессмысленно — получим статистику
таймаутов вместо статистики брака. Замер ждёт, пока `moderate` не вернётся к 3–4 с.
Провалы денег не стоят (вендор не выставляет счёт за несостоявшийся кадр), стоят времени.

**Следствие для варианта I:** диктовка делает промпт ещё длиннее, а он уже подошёл к потолку
в 60 с. Это довод в цену пути I на шаге 5, и его нельзя проверить иначе как замером времени
на том же входе.

## Лог вендора: время уходит не в модель (2026-08-30)

Пользователь включил на стороне вендора запись запросов, и проба `cardtext-dictated-probe`
впервые дала обе стороны одного и того же вызова.

| Вызов | Вендор пишет у себя | Мы пишем в `generation_costs` |
| --- | --- | --- |
| `/v1/images/generations` | **8 969 мс** | **47 356 мс** |
| `/v1/chat/completions` (модерация) | 871–1 748 мс | 8 930 мс |
| `/v1/chat/completions` (тексты) | 1 066 мс | 1 773 мс |

По меткам времени: наш вызов изображения начался ≈18:17:17, вендор принял запрос в 18:17:23
(шесть секунд ушло на отправку фото), ответил к 18:17:32 — а наша запись закрылась в 18:18:04.
**Тридцать две секунды после того, как вендор закончил.**

**Что это отменяет.** Вывод предыдущего раздела — «промпт стал длиннее, модель думает дольше»
— не подтверждается: на модель приходится девять секунд из сорока семи. Разрыв виден и на
модерации, где промпт не менялся вообще (8,9 с у нас против 1,7 с у вендора), значит дело не
в длине задания и не в размере ответа: там ответ — три слова.

**Что это значит для B13.** Потолок в 60 с съедается не генерацией, а нашей стороной провода:
доставкой тела ответа, изоляцией edge-рантайма, локальным Docker на Windows — что именно,
этими данными не различить. Но искать надо там, а не в промпте. Цена вопроса меняется: если
это транспорт, вариант I не становится медленнее от диктовки, и довод против него отпадает.

**Чего этот лог НЕ говорит.** Запись включена только что, прогонов базы и перезамера в ней
нет — сопоставить с ними нечего. И «время отв.» вендора может измерять не то же, что наш
секундомер (например, до первого байта, а не до последнего). Прежде чем строить на этом
решение, нужно померить наш путь по частям — время до первого байта и время чтения тела
отдельно.

## Замер диктовки: содержание вылечено, отрисовка — нет (2026-08-30)

Прогон `bench/runs/cardtext-dictated-20260830T144441/`, **только `food-pepsi`, 5 повторов**,
47,30 ₽. Выборка урезана до одного набора не из соображений методики, а по деньгам: на счету
вендора оставалось 67 ₽. Взят самый бракованный набор базы (4 кадра из 5) и самый дешёвый по
бакету. Четыре кадра получено, пятый — отказ вызова.

### Один и тот же товар в трёх состояниях промпта

| Код | База | После правки формулировок | С диктовкой |
| --- | --- | --- | --- |
| A искажение или дублирование слова | 0 / 5 | 1 / 5 | **2 / 4** |
| B невыполненный плейсхолдер | 1 / 5 | 0 / 5 | 0 / 4 |
| F выдуманная характеристика | 0 / 5 | 1 / 5 | 0 / 4 |
| G служебные лейблы шаблона | 2 / 5 | 1 / 5 | 0 / 4 |
| H текст не на русском | 3 / 5 | 0 / 5 | 0 / 4 |
| Кадров с дефектом | 4 / 5 | 3 / 5 | **2 / 4** |

### Что видно

**Классы содержания закрыты полностью.** B, F, G, H — ноль. Это и ожидалось: сочинять модели
больше нечего, текст приходит готовым. Проверено буквой: в r3 нарисовано «В упаковку входит
12 жестяных банок объемом для удобного запаса» — коряво, и ровно так же коряво лежит в базе.
Модель воспроизвела наш текст дословно, включая нашу же неловкую формулировку. Это не брак
изображения, это качество `composeCard` (скоуп M6).

**Класс A стал хуже, а не лучше: 0 → 1 → 2.** В r1 — «с насыщенным вкусом / насыщенный
вкусом», «нотками французской / французской ванили», «для удобного хранего хранения». В r4
задвоено «Газированный напиток напиток» и разорван заголовок. Оба чистых кадра (r2, r3)
воспроизводят текст точно.

**Механизм правдоподобен и неприятен.** Диктовка заставляет модель нарисовать **весь** наш
текст — три предложения, ~250 знаков. Раньше она сочиняла сама и писала короче, а короче —
меньше глифов и меньше поводов сбиться. То есть мы обменяли дефекты содержания на дефекты
отрисовки, и обмен получился не бесплатным.

**Выборка мала — 4 кадра.** Утверждать «A вырос вдвое» на таких числах нельзя; можно
утверждать, что диктовка его точно **не лечит**, а это и есть ответ на вопрос шага 3(в).

### Что из этого следует для развилки

Дефекты отрисовки ни одной правкой промпта не убираются в принципе: буквы рисует модель
изображений, и она иногда ошибается. Значит остаточный риск варианта I — это не «редкий
казус», а половина кадров на длинном тексте. Отсюда два хода, которых до замера не было:

1. **Отдавать в кадр только заголовок, а описание оставить рядом с изображением** — так его
   уже показывает `Generation.tsx`, и FR-07 этого не запрещает (Context п.3). Меньше знаков —
   меньше поводов сбиться, и самая заметная строка карточки под контролем.
2. **Своя вёрстка (пути II и III)** снимает класс A целиком, а не снижает его частоту: текст
   рисуется шрифтом, а не моделью.

Ход 1 стоит одной строки в промпте, ход 2 — отдельной работы. Оба идут в ADR шага 5.

## Поиск готовых решений: интерактивная починка блока (2026-08-30)

Внесено по вопросу пользователя на шаге 5: нет ли решений, делающих отрисованную карточку
интерактивной — ткнул в бракованный блок, и он чинится. Две теории пользователя проверены на
существующих продуктах **до** оценки «сделать с нуля».

### Теория 1: вендор перерисовывает только испорченный блок

**Поддержано технически.** `gemini-3.1-flash-image` — это Nano Banana 2, и она умеет
редактирование по маске и без маски (правка описывается словами, область модель находит сама).
Наш код уже шлёт исходники полем `input_references`, так что отправка готовой карточки обратно
референсом — правка одной функции, а не новый транспорт.

**Прецедент:** [Type-R](https://arxiv.org/html/2411.18159) — тот же конвейер целиком: найти
опечатку в сгенерированном изображении, стереть текст, заново сгенерировать бокс, поправить
слово инпейнтингом. Коммерческие аналоги того же класса — ImagineArt Magic Text, Fotor,
RemoveTexts.

**У нас автоопределение дешевле, чем у них.** Им нужен словарь и эвристики; у нас есть эталон —
`composeCard` вернул точный текст ДО отрисовки. Достаточно прочитать кадр дешёвой моделью и
сравнить строки.

**Дыра теории.** Починка — новый бросок теми же костями и той же моделью, которая сделала
опечатку. Гарантии нет. И брать за это баллы клиента нельзя: человек платит за корректную
карточку, а не за попытки её получить. Если делать — за наш счёт, и тогда это не продуктовая
функция, а внутренний ретрай.

### Теория 2: двойной клик снимает текст и отдаёт редактируемый бокс

Класс продуктов существует (OCR → инпейнтинг фона → подбор шрифта → текстовый слой). Но это
**путь III с дорогим крюком**: если конечное состояние — редактируемый текстовый бокс поверх
изображения, дешёвый способ его получить — не давать модели рисовать текст вообще.
Восстанавливать шрифт, кегль и позицию из растра и замазывать дыру — работа, которой не
существовало бы при своей вёрстке с самого начала.

### Побочная находка, которая может отменить развилку: дело в модели

В независимых сравнениях Nano Banana называют среди моделей, «пишущих бессмысленные символы»,
а Ideogram — как чисто рисующую настоящие слова, включая кириллицу. Класс A — свойство нашего
выбора модели, а не природы задачи.

На том же шлюзе — **по его публичному каталогу** (`GET /public/aitunnel/models/images`, без
ключа), а не по обзорам:

| Модель | Цена за кадр | Разрешения | Референсов |
| --- | --- | --- | --- |
| **наша** `gemini-3.1-flash-image` | 5,10–22,10 ₽ (мы платим 11,55 на 1K, 17,3 на 2K) | 512/1K/2K/4K | 14 |
| `gemini-3.1-flash-lite-image` | **2,89 ₽ плоско** | только 1K | 14 |
| `qwen-image-3` | 5,10–7,14 ₽ | 1K/2K | 4 |
| `seedream-4.5` | **6,80 ₽ плоско** | 1K/2K/4K | 14 |
| `seedream-5-0-pro` | 7,65–22,44 ₽ | 1K/2K | 14 |
| `flux.2-pro` | 5,44–11,90 ₽ | не объявлены | 8 |
| `gpt-image-2` | 1,53–32,13 ₽ | не объявлены, но есть `quality` | 16 |
| `recraft-v4.1` | 6,80 ₽ плоско | не объявлены | **1** |

**Поправка к первой редакции этого раздела: Ideogram на шлюзе нет вообще.** Цена «5 ₽»
попала сюда из стороннего обзора, а в каталоге из 27 моделей его не значится. Проверять надо
было каталогом сразу.

**Что каталог дал сверх ожидаемого:** референсные фото принимают **все 27 моделей** — риска
несовместимости по этой оси нет. Ограничение другое: `recraft-v4.1` берёт только один
референс, а `seedream-5-0-lite` не поддерживает 1K, то есть под набор `food-pepsi` (1024 ×
1024) не годится.

Мы платим больше всех за модель, которая хуже всех пишет буквы. **ADR-0010 выбирал её по
узнаваемости товара (6 из 7), текст критерием не был** — это записано там честно, и упрёка к
тому решению нет. Появился новый критерий, которого тогда не измеряли.

Смена модели — переменная окружения `AI_PROVIDER_IMAGE_MODEL`, не правка кода.

**Чего эта находка НЕ говорит.** Что чужая модель удержит товар узнаваемым на референсном
фото — неизвестно, а это критерий ADR-0010 и он важнее текста: карточка с чужим товаром
бесполезна независимо от букв. Ideogram в доках шлюза среди умеющих референсы не назван.
Проверяется только замером.

## Проба шести моделей: гипотеза про модель не подтвердилась (2026-08-30)

Прогоны `bench/runs/model-probe-20260830T152648` и `…T153212`, по одному кадру на модель,
вход — набор `food-pepsi` и тексты кадра r1 прогона `cardtext-dictated` (того, где наша модель
написала «для удобного хранего хранения»). Потрачено **41,81 ₽**.

| Модель | Цена | Время | Текст |
| --- | --- | --- | --- |
| `gpt-image-2` | **10,18 ₽** | 48,7 с | **безупречно, дословно** |
| **наша** `gemini-3.1-flash-image` | 11,56 ₽ | **14,9 с** | **безупречно, дословно** |
| `seedream-4.5` | 6,81 ₽ | 42,8 с | латиница вместо кириллицы: «12 банок **no** 355 **m**л», «франц**y**зской»; **банка пропала из кадра** |
| `seedream-5-0-pro` | 7,65 ₽ | 109,9 с | восемь ошибок: «Гарираванный наниток», «франковской ванили», «пололекет наснаждатьься», «капорий» |
| `qwen-image-3` | 5,61 ₽ | 54,0 с | нечитаемо: заголовок «French Viailla», описание — наложенные друг на друга строки |
| `flux.2-pro` | — | 7,7 с | **заблокирован модерацией** Black Forest Labs на бренде Pepsi |

### Гипотеза опровергнута, и это главный результат

Обзоры, из которых она выросла, называли Nano Banana среди «пишущих мусорные символы».
**На кириллице расклад обратный:** наша модель — одна из двух чистых, а все три более дешёвые
альтернативы дали брак заметно хуже нашего худшего кадра. Чужой бенчмарк мерил латиницу и на
нашу задачу не переносится. Ошибка была в том, что я взял его выводы, а не проверил на своём
входе.

**Экономия отменяется вместе с гипотезой.** Дешёвые модели дешевле не просто так: две чистые —
это две самые дорогие из проверенных.

### Что узнали про наш собственный код

Три модели отвергли наше тело запроса, и все три отказа бесплатны:

- `gpt-image-2` и `flux.2-pro` не принимают ни `resolution`, ни `aspect_ratio` — они
  OpenAI-совместимые и ждут `size` в пикселях («1024x1024»). **Наш `resolutionParam` умеет
  только форму бакетов**, то есть переход на `gpt-image-2` — это не смена переменной
  окружения, а правка провайдера.
- `flux.2-pro` дальше отвалился по модерации вендора на бренде Pepsi. Для сервиса, который
  делает карточки брендового товара, это дисквалификация независимо от качества текста.

### Чего проба НЕ говорит

**По одному кадру частоту не мерить.** Наша модель дала чистый кадр там, где на четырёх
предыдущих кадрах давала два бракованных — при базовой частоте около половины чистый кадр
ничем не удивителен. Ранжировать `gpt-image-2` и нашу между собой по одной пробе нельзя;
можно только отделить их обеих от трёх явно худших, и вот это отделение однозначно.

### Что это значит для развилки

Смена модели **не закрывает класс A** — она либо не меняет ничего (`gpt-image-2` не проверен
на частоте), либо делает хуже (три остальных). Довод в пользу своей вёрстки от этого крепнет:
единственный путь, снимающий класс A целиком, а не понижающий его частоту, — не давать модели
рисовать буквы.

## Этап B: замер частоты на двух чистых моделях (2026-08-30)

Прогоны `model-probe-20260830T162653` (gpt-image-2 × 5), `…T163015` (наша × 5) и `…T163410`
(gpt-image-2 на кадре 3 : 4). Тот же вход, тот же пробник, разметка глазами. **93,79 + 12,56 ₽.**

| | `gpt-image-2` | **наша** `gemini-3.1-flash-image` |
| --- | --- | --- |
| кадров с дефектом текста | **1 из 7** | **4 из 6** |
| заголовок искажён | **0 из 7** | 2 из 6 |
| потерян объект из кадра | **0 из 5** | 3 из 5 |
| цена, бакет 1K | 5,21–10,18 ₽ | 11,56 ₽ |
| цена, кадр 3 : 4 | 12,56 ₽ (1536 × 2048) | 17,26–17,42 ₽ (1792 × 2400) |
| время вызова | 31–52 с (среднее 39) | **13,7–15,3 с (среднее 14,6)** |
| форма запроса | `size` в пикселях | `resolution` + `aspect_ratio` |

Где именно у нашей модели: #1 «и нот-французской» (съедено «ками») и «насiаждаться» с
латинской i; #2 «Газирова**н**ый» — пропала одна «н»; #3 заголовок «Diet Pepsi Jazz **Blacky**
French Vanilla»; #5 заголовок «Diet Pepsi Jazz **Blakrn Va**». Чистый только #4. У
`gpt-image-2` единственный дефект — «позволяет **наслаждатся**» на кадре 3 : 4.

### Что это значит

**Разрыв велик, но выборка мала.** 1 из 7 против 4 из 6 — точный тест Фишера даёт p ≈ 0,03
односторонне. Сигнал уверенный, доказательством считать рано: это **один товар**.

**Заголовок и описание ведут себя по-разному.** Все дефекты `gpt-image-2` — в длинном
описании; заголовок верен во всех семи кадрах. Наша модель коверкала и заголовок. Это прямо
поддерживает ход «в кадр — только заголовок, описание рядом с изображением»: короткая строка
у обеих моделей выходит надёжнее длинной, а у `gpt-image-2` на нашей выборке — безошибочно.

**Кадр 3 : 4 доступен.** `gpt-image-2` вернул 1536 × 2048 — ровно 3 : 4 и выше порога
900 × 1200, то есть профили `ozon × одежда/аксессуары` он закрывает, причём дешевле нашей
модели (12,56 против 17,4 ₽).

**Цена смены — правка провайдера, не переменной окружения.** `gpt-image-2` не принимает ни
`resolution`, ни `aspect_ratio`; `resolutionParam` умеет только форму бакетов. Плюс потеря
скорости почти втрое.

### Чего этим НЕ доказано

**Узнаваемость товара на широкой выборке.** ADR-0010 выбирал нынешнюю модель по 7 разным
товарам (6 из 7). Здесь один товар — на нём `gpt-image-2` держал оба объекта 5 из 5 против
2 из 5, но переносить это на семь категорий нельзя. **Смена модели не может быть решена этим
замером** и требует своего: 7 наборов × ≈ 10 ₽ ≈ 70–85 ₽.

## Замер узнаваемости: gpt-image-2 на семи наборах (2026-08-30)

Прогон `bench/runs/model-probe-20260830T164404`, все семь наборов стенда, блок из **одного
заголовка** (кандидат к внедрению). **26,02 ₽** — вдвое дешевле ожидаемого: короткий промпт
даёт 2,51–4,10 ₽ на кадре против 10,11 ₽ на длинном.

| Набор | Итог | Заголовок | Товар |
| --- | --- | --- | --- |
| `accessories-sunglasses` | 2,60 ₽ | дословно | узнаваем |
| `accessories-watch` | 4,10 ₽ | дословно | узнаваем, «РАКЕТА» на циферблате читается |
| `beauty-cream` | 2,51 ₽ | дословно | узнаваем |
| `clothing-shoes` | 4,10 ₽ | дословно | узнаваем, логотип New Balance на месте |
| `food-pepsi` | 10,11 ₽ | дословно | узнаваем, оба объекта |
| `home-chair` | 2,60 ₽ | дословно | узнаваем, **цвета обивки сдвинуты** |
| `other-bobblehead` | — | — | **отказ системы безопасности OpenAI** |

**Заголовок дословен 6 из 6, товар узнаваем 6 из 6.** С учётом прошлых прогонов короткая
строка у `gpt-image-2` верна **13 раз из 13**.

### Отказ — главная находка, и он воспроизводим

`other-bobblehead` (коллекционная фигурка Стар-Лорда) отклонён системой безопасности OpenAI.
Повтор дал тот же отказ — это правило, а не случайность. Отказы бесплатны, но продукту от
этого не легче: **сервис не сможет сделать карточку лицензионного мерча с персонажами
никогда**, а это целая товарная категория. Наша нынешняя модель этот же набор отрисовывала
(замер ADR-0010, 7 кадров из 7 доставлено).

Formально счёт одинаков — 6 из 7 у обеих моделей. Но промахи разной природы: у нашей это
качество кадра, у `gpt-image-2` — глухой отказ по классу товара. Второе не лечится ни
промптом, ни повтором.

### Общий сдвиг цвета на `home-chair` — не в пользу ни одной модели

Исходная обивка: чёрная, лаймово-жёлтая, приглушённо-красная. `gpt-image-2` нарисовал чистый
жёлтый и малиновый — тот самый сдвиг к «флажным» цветам, против которого в промпте есть
оговорка со времён M5. Наша модель спотыкалась здесь же. Общая трудная точка.

### Сводка по деньгам за весь шаг 5

| Прогон | Потрачено |
| --- | --- |
| Проба шести моделей | 41,81 ₽ |
| Этап B, две чистые модели × 5 | 93,79 ₽ |
| Проверка кадра 3 : 4 | 12,56 ₽ |
| Узнаваемость, семь наборов | 26,02 ₽ |
| **Итого** | **174,18 ₽** |

## Подтверждающий прогон через боевой конвейер (шаг 6, 2026-08-31)

`bench/runs/gpt-image-2-20260830T194423`, три набора, **24,58 ₽** (изображения 24,20 ₽,
тексты 0,38 ₽). Первый запуск в тот же вечер провалился на всех трёх наборах и **не стоил
ничего**: из `.env` пропали `AI_PROVIDER_BASE_URL`, `AI_PROVIDER_API_KEY` и
`AI_PROVIDER_TEXT_MODEL`, `requireConfig` упал раньше сети. Баллы при этом не списались, а
пользователю показали «Генерация временно недоступна» — поведение верное, бага нет.

| Набор | Модель | Кадр | Цена | Заголовок | Товар |
| --- | --- | --- | --- | --- | --- |
| `clothing-shoes` | `gpt-image-2` | 1536 × 2048 png | 2,53 ₽ | дословно | узнаваем, логотип на месте |
| `food-pepsi` | `gpt-image-2` | 1024 × 1024 png | 10,12 ₽ | дословно | узнаваем, **оба объекта** |
| `other-bobblehead` | отказ → `gemini-3.1-flash-image` | 896 × 1200 png | 11,55 ₽ | дословно | узнаваем |

**Три из трёх: заголовок дословен, товар узнаваем, лишнего текста в кадре нет.** Обе формы
запроса отработали: пиксельная (`size`) на квадрате и на 3 : 4, бакетная — на резерве.
`describeProfileMismatch` пропустил все три кадра, включая 1536 × 2048 против целевых
1792 × 2400 (порог площадки 900 × 1200 соблюдён).

### Ветка резерва отработала на живом отказе

Не в тесте, а в конвейере, строкой из лога функций:

```
[Warning] AITunnel gpt-image-2 отказ по содержанию за 24941 мс — повтор на резервной модели
[Info]    AITunnel gemini-3.1-flash-image 14999 мс cost_rub 11.55 запрошено 896×1200 получено 896×1200 png
```

Отказ бесплатен — в логе вендора у него 400 и пустая стоимость. Пользователь получил карточку
лицензионного мерча, то есть товарная категория, которую смена модели отнимала, вернулась.
Заплатил он за один кадр, а не за два.

### Разрыв B13 не воспроизвёлся — и это меняет прошлый вывод

Лог вендора против наших записей на тех же четырёх вызовах:

| Вызов | Вендор | Наша запись | Разница |
| --- | --- | --- | --- |
| `gpt-image-2`, кроссовки | 20 981 мс | 26 775 мс | +5,8 с |
| `gpt-image-2`, Pepsi | 39 448 мс | 41 921 мс | +2,5 с |
| `gpt-image-2`, отказ | 23 789 мс | 24 941 мс | +1,2 с |
| `gemini-3.1-flash-image`, резерв | 12 923 мс | 14 999 мс | +2,1 с |

**1,2–5,8 с вместо тридцати двух.** Прошлый вывод B13 («узкое место — наш стек») держался на
сравнении, разнесённом во времени: пробник мерил в 15:26, а конвейерные 47 с взяты с
перезамера в 11:58 и вечернего эпизода накануне. Одновременного сравнения не было, и сегодня
разрыв не появился. Это не значит, что вечернего разрыва не было, — значит, он не постоянное
свойство стека. B13 остаётся открытым, но его первый шаг диагностики меняется: сначала
поймать разрыв повторно, а уже потом искать его причину в изоляции Deno и Docker.

Потолок `IMAGE_TIMEOUT_MS` из-за этого сведён со 150 с до **120 с** — вдвое над худшим
наблюдённым вызовом (51,9 с), без запаса на разрыв, существование которого под вопросом.

### Что прогон НЕ закрыл

- **NFR-01 на `clothing-shoes`** — распознавание 6 689 мс при бюджете 5 с. Это B12, известная
  запись, не находка этого прогона.
- **Частота брака на новой модели в конвейере** — три кадра не выборка. Считать её сейчас
  незачем: замер шага 5 на семи наборах уже дал 13 дословных заголовков из 13, а конвейер
  посылает вендору тот же промпт, что и пробник.

## Цена вариантов развилки (заготовка к шагу 5)

Разобрано 2026-08-30, **до** замера: цена вариантов от измеренной частоты не зависит, от неё
зависит только выбор. Замер добавит к этой таблице остаточный риск варианта I.

**I. Вендор рисует текст, промпт исправлен.**
Цена — правка одной функции (`imagePrompt`) и два платных прогона. Риск остаётся ненулевым
по самой природе решения: недетерминированную генерацию нельзя загнать в ноль формулировкой,
можно лишь снизить частоту. Годится, если после шага 4 категории B и C уходят в ноль, а
остаток — только A и D.

**II. Своя вёрстка в Edge Function.**
Требует того, чего в проекте нет и не может появиться дёшево:
- **декодер входного кадра** — вендор отдаёт и JPEG, и PNG (на семи одинаковых запросах —
  3 и 4 соответственно, см. шапку `_shared/image.ts`), значит нужны оба;
- **честный кодировщик JPEG** — нынешний `_shared/jpeg.ts` кодирует только изображения из
  одноцветных блоков 8×8, где ДКП вырождается в один коэффициент. Для фотографии он
  непригоден: нужны настоящее ДКП, зигзаг, кодирование серий, таблицы из приложения K;
- **растеризатор шрифта** — разбор TTF или встроенный растровый шрифт, плюс перенос строк.

Всё это вручную, потому что зависимостей в функциях нет намеренно (B9). Объём несопоставим
с задачей: класс дефектов ценой в собственный графический стек.

**III. Своя вёрстка в браузере через Canvas 2D.**
Тексты уже в БД (`card_title`, `card_description`), Canvas — нативный API, новых зависимостей
нет (в духе `ponytail`: native прежде dep). Что придётся тронуть:
- `downloadResult` ([api.ts:291](../../src/features/generation/api.ts#L291)) — сейчас отдаёт
  блоб из бакета как есть; композиция вставляется между загрузкой и `link.click()`;
- превью на экране результата ([Generation.tsx:241](../../src/screens/Generation.tsx#L241)) —
  иначе разойдётся с файлом и сломается FR-15 («скачанный файл соответствует показанному»);
- каталог — те же две точки для повторного скачивания (FR-17);
- FR-25 после переупаковки — размер не меняется, формат становится JPEG, вес падает; но
  проверка профиля сейчас живёт на сервере (`describeProfileMismatch` в `generation-worker`),
  и её клиентскую половину надо либо завести, либо обосновать, почему не нужна.

**Подводный камень III:** системный шрифт на машине пользователя. Один и тот же результат
выглядел бы по-разному в Windows и в macOS, а в каталоге — иначе, чем при скачивании.
Лечится шрифтом, поставляемым с приложением (`@font-face` из бандла), а не системным —
это фронтенд, не Edge Function, там зависимость обычная.

## Решение шага 5: путь I со сменой модели (2026-08-31)

Записано в [ADR-0011](../../docs/adr/0011-image-model-gpt-image-2-with-fallback.md).
Текст на карточке продолжает рисовать вендор; модель изображений меняется на `gpt-image-2`;
нынешняя `gemini-3.1-flash-image` остаётся в конфигурации **резервом на отказ по контенту**.

**Что решило дело.** Замер узнаваемости оставил размен: лучший текст и цена против потерянной
товарной категории (лицензионный мерч, глухой отказ OpenAI). Резерв этот размен снимает —
модель уже настроена, оплачена тем же ключом и этот набор отрисовывает. Отказ по авторскому
праву объясним клиенту честно, но объяснять его, имея работающую замену в конфиге, нечем
оправдать.

**Путь III отвергнут не только по цене.** Аргумент пользователя 2026-08-31: редактор шрифтов
в руках без насмотренности даёт уродливый результат, за который винят сервис, — тогда как
правку промптом пользователь делает своей волей и своей ответственностью. Замер снял и
техническую нужду в III: короткая строка у `gpt-image-2` верна 13 раз из 13.

**Развилка, ушедшая в бэклог.** Платная правка кадра по требованию пользователя — B14
(`planning/BACKLOG.md`) и строка в разделе «Дальнейшие апгрейды» генплана. Технически
подтверждено (`supports_edit: true` у обеих моделей, `input_references` уже в адаптере), но
не измерено главное — сохраняет ли перерисовка остальной кадр неизменным.

**Предупреждение к шагу 6.** Новая модель отвечает за 31–52 с против 14 с у нынешней, а наш
конвейер добавляет ~32 с сверх провода (B13). Сумма пробивает `IMAGE_TIMEOUT_MS = 60_000`.
Смена модели без правки потолка сделает хуже, а не лучше.

## Verification

- `bench/README.md` содержит таксономию брака **и** список того, что в неё не входит;
  раздел датирован до первого замера (проверяется порядком коммитов).
- `npm run bench -- --label <кандидат> --repeat 5 --only accessories-watch` отрабатывает и
  кладёт в `report.html` пять кадров одного набора рядом.
- В плане записаны две таблицы частот по категориям — «до» и «после» правки промпта — с
  числом прогонов, а не с оценкой словами.
- Категории (B) невыполненный плейсхолдер и (C) чужой брендинг равны нулю на замере после
  правки; иначе шаг 5 обязан выбрать путь II или III, а не путь I.
- ADR существует, перечисляет три варианта с ценой и называет отвергнутые.
- `npm test` и `npm run lint` зелёные; изменения в `aitunnel.ts` не задели ветку
  `kind === 'photo'` (проверяется прогоном набора с `kind: photo`, если он появится, либо
  чтением диффа).

## Открытые вопросы

- **Бюджет.** План закладывает ~350 ₽ на два замера (шаги 2 и 4). Меньшая выборка при
  недетерминированном браке ответа не даст — сокращать её осознанно или не сокращать вовсе.
  Решение — за пользователем на шаге 2.
- **Путь III и хранилище.** Если вёрстка уезжает в браузер, в бакете `results` остаётся кадр
  без текста. Считается ли это «изображением карточки» для FR-07 и FR-17 (повторное
  скачивание из каталога) — решается в ADR шага 5, не раньше.
- **Порог приёмки для категории (A).** Искажение слова в декоративной надписи и искажение в
  заголовке товара — разной тяжести. Нужен ли раздельный порог, станет видно на числах
  шага 2.
