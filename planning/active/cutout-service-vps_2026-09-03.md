# Сервис выреза и сборки на VPS — отдельное приложение со своим репозиторием

Status: ACTIVE (с 2026-09-03)

> План под шаг B4 плана [`card-assembly-pipeline_2026-08-31.md`](card-assembly-pipeline_2026-08-31.md).
> Исполнение — **после апрува пользователя**, и часть шагов заблокирована его же действиями
> в панели Beget: агент не может ни зарегистрироваться, ни оплатить, ни нажать «создать
> сервер». Что именно делает человек — отмечено `TODO(пользователя)`.

## Context

[ADR-0015](../../docs/adr/0015-card-service-on-vps-not-edge-function.md) увёл сборку карточки
и вырез из Edge Function в свой сервис: сборка не помещается в процессорное время изолята
(19–21 макет из 34, медиана 1657 мс при жёстком пределе около двух секунд), а вырез не
помещается тем более. Сервиса пока не существует — есть контракт
([ADR-0014](../../docs/adr/0014-cutout-runner-onnx-behind-interface.md)), оснастка замера и
измеренная модель.

**Что уже известно из замеров и не нужно выяснять заново** (замер 2026-09-03, коммит `527ec08`):

| Факт | Число | Следствие |
| --- | --- | --- |
| Кромка `birefnet-general-lite` | полутон 0,2–0,4% | модель по умолчанию из ADR-0015 подтверждена |
| Память модели при выключенной арене ORT | **пик 597 МБ** | памяти хватает даже на 2 ГБ |
| Память при арене по умолчанию | **пик 12 083 МБ** | на 4 ГБ сервис убьёт OOM-killer |
| Инференс, 1 / 2 / 4 / 8 потоков | 24 / 15,8 / 13,0 / 12,0 с | отдача падает после четырёх ядер |
| Загрузка сессии | 5,0 с | модель обязана жить в процессе резидентно |

Отсюда два вывода, которые план обязан довести до кода: **арена ORT выключается явно**
(`enableCpuMemArena: false`) — это требование к развёртыванию, а не настройка на вкус; и
**ограничение — процессорное время, а не память**.

## Цель

Работающий сервис на своей коробке: принимает кадр — отдаёт маску; живёт на поддомене
второго уровня под TLS; настроен по базовой защите из бесплатных средств; собран отдельным
приложением в отдельном репозитории [`ovr58/cutout_runner`](https://github.com/ovr58/cutout_runner)
(сейчас пуст). Со стороны Merch Kit — шов `CutoutRunner` и вызов сервиса, без утечки в код
знания о том, где именно он считает.

## Решения пользователя (2026-09-03)

| Вопрос | Решение |
| --- | --- |
| Форма фиксации замера и коробки | правка [ADR-0015](../../docs/adr/0015-card-service-on-vps-not-edge-function.md) **на месте** — ADR сам обещал уточнение на первом прогоне |
| Домен | купить `.ru` на Beget, 199 ₽/год; на нём же потом прод |
| Доступ к коробке | агент работает **по SSH с машины разработки**; регистрация, оплата и панель — за пользователем |
| Тариф VPS | `TODO(пользователя)`: рекомендация — **2 vCPU / 4 ГБ / 40 ГБ**, 33 ₽/сутки + 5 ₽/сутки за IPv4 ≈ 1 140 ₽/мес |

**Тариф — не «сколько памяти», а «сколько секунд».** Память из выбора выпала: 600 МБ влезает
всюду. За 2 vCPU вырез считается ~16 с на машине разработки и, с поправкой на слабее ядро
VPS, ~20–24 с на коробке; 4 vCPU дают ~17–20 с за вдвое большие деньги. Вырез нужен 4 макетам
из 34, генерация асинхронная, и вендор отвечает дольше — поэтому рекомендация младшая.
Расширение на Beget — изменение конфигурации, а не переезд.

**Чего покупать не надо:** «Виртуальный хостинг» (420–790 ₽/мес) — там нет ни root, ни своих
демонов, ни нативных библиотек; «Серверы» в меню — это выделенные физические машины от
11 100 ₽. Нужен раздел **«Облако» → серверы VPS**.

## Что агент сделать не может — и как это обходится

| Действие | Кто | Почему |
| --- | --- | --- |
| Регистрация на beget.com, оплата, создание VPS, покупка домена, DNS-записи | пользователь | у агента нет браузера и доступа к учётной записи; это внешние действия с деньгами |
| Создание SSH-ключа и загрузка публичной части на коробку | пользователь | приватный ключ не должен проходить через диалог |
| Создание репозитория на GitHub, доступ к приватному `project-template` | пользователь | `gh` в сессии не авторизован (`gh auth status` — не залогинен), приватный репозиторий агенту не виден |
| Настройка ОС, развёртывание, проверки на коробке | агент по SSH | после того, как ключ пущен на коробку |

> `TODO(пользователя)`: содержимое [`ovr58/project-template`](https://github.com/ovr58/project-template)
> агенту недоступно (репозиторий приватный, отдаёт 404 без авторизации). Шаг 5 написан так,
> чтобы шаблон был подставим: сначала читаем, что в нём есть, потом кладём поверх сервис.

## Критичные файлы

**В репозитории Merch Kit:**

| Путь | Что с ним |
| --- | --- |
| `docs/adr/0015-card-service-on-vps-not-edge-function.md` | правка: измеренные числа вместо оценки, требование выключенной арены, выбранный тариф |
| `docs/adr/0016-*.md` | новый: граница доверия сервиса (кто и чем доказывает право звать) |
| `supabase/functions/_shared/card-layout/cutout.ts` | новый: тип `CutoutRunner` и реализация поверх HTTP |
| `supabase/functions/_shared/card-layout/render.ts` | вызов раннера; «маски нет» остаётся штатным исходом (K-3) |
| `.env.example`, `supabase/config.toml` | адрес сервиса и имя секрета — без значений |
| `planning/active/card-assembly-pipeline_2026-08-31.md` | отметка B4 по мере закрытия |

**В репозитории сервиса** (`ovr58/cutout_runner`, отдельное приложение):

| Путь | Что это |
| --- | --- |
| `src/server.ts` | HTTP-слой: `/health`, `POST /cutout` |
| `src/cutout.ts` | инференс: сессия ORT резидентно, арена выключена, потоки из конфигурации |
| `deploy/cutout-runner.service` | unit systemd с ограничениями прав и памяти |
| `deploy/nginx.conf` | TLS-терминатор, лимит размера тела и частоты запросов |
| `deploy/README.md` | воспроизводимая установка на чистую Ubuntu |
| `PLAN.md` | **наряд**: контракт, решения и приёмка — всё, что нужно сессии в отдельном окне, не видящей Merch Kit |

## Шаги

- [ ] **0. Предусловия — за пользователем.** `TODO(пользователя)`. Аккаунт заведён, баланс
  пополнен на 1 000 ₽ (2026-09-03). Осталась форма создания сервера
  (`cp.beget.com/cloud/servers/create`) — что в ней выбирается:

  | Поле | Выбор | Почему |
  | --- | --- | --- |
  | Регион | Россия, Санкт-Петербург | единственный в списке |
  | Конфигурация | вкладка «Фиксированная», **2 ядра / 4 ГБ / 40 ГБ**, 33 ₽/день | «Произвольная» дороже за те же ресурсы; High-Freq покупает секунды, а не разы |
  | Образ | **Ubuntu 26.04**, бесплатно | чистая ОС |
  | Панели (ispmanager, Hestia, FASTPANEL, BitrixVM) | **не брать** | забирают порты, свой nginx и свои правила firewall — то самое, что шаг 6 настраивает руками; плюс лишняя поверхность атаки |
  | Публичный IPv4 | **новый**, 5 ₽/день | без него к сервису не подключиться |
  | Приватная сеть | выключено | соединять не с чем |
  | Аутентификация | **только SSH-ключ**; «Задать пароль» не трогать | без способа входа Beget пришлёт пароль на почту, и доступ к серверу окажется в почтовом ящике |
  | Имя хоста | `cutout-runner` | в «Дополнительных параметрах» |

  Итого 38 ₽/день — тысячи хватает примерно на 26 дней.

  **Ключ выпущен 2026-09-03:** `ed25519`, приватная часть — `~/.ssh/beget_cutout`, права
  сведены к одной учётной записи через `icacls`; публичная вставляется в панель. Пароля на
  ключе нет сознательно: агент ходит на коробку неинтерактивно, и защита ключа — права на
  файл и доступ к машине разработки.

  Дальше: покупка домена `.ru` → A-запись поддомена сервиса на IP коробки.
  На выходе агенту нужны: **IP, имя поддомена, имя пользователя на коробке**.
  **Пароль root в диалог не передавать** — вход только по ключу.

- [ ] **1. Правка ADR-0015 — числа вместо оценки.** Строка «Оценка коробки — от 2 vCPU и
  4 ГБ; это расчёт из замеренных чисел… уточняется на первом же прогоне» заменяется таблицей
  из раздела Context, а рядом встаёт требование: **арена ORT выключается явно**, иначе
  процесс занимает всю доступную память и коробка любого размера уходит в OOM. Туда же —
  выбранный тариф с ценой и оговорка, что предел — процессорное время. Решение ADR не
  меняется, заполняется обещанный им пробел. Не требует коробки.

- [ ] **2. ADR-0016 — граница доверия сервиса.** ADR-0015 прямо предупреждает: Edge Function
  получала границу от платформы, у своего сервиса её нет. Предлагаемое решение, которое ADR
  и зафиксирует:
  - **сервис не имеет ключей проекта** — ни service-role, ни доступа к Postgres, ни к
    Storage. Он получает байты кадра и отвечает байтами маски. Компрометация коробки не даёт
    доступа к данным пользователей — это главное свойство, ради которого стоит терпеть
    пересылку байтов вместо ссылки;
  - **вызывающий доказывает право общим секретом** в заголовке поверх TLS; секрет живёт в
    секретах Supabase и в файле окружения на коробке с правами `600`. Список разрешённых
    адресов не годится: исходящие адреса Edge Functions не закреплены;
  - **лимит размера тела и частоты** на стороне nginx, а не приложения.
  Не требует коробки.

- [ ] **3. Шов `CutoutRunner` в Merch Kit.** Ровно то, что предписал шаг B4: **тип функции, а
  не интерфейсный слой** — `export type CutoutRunner = (frame: ImageRef) => Promise<ImageRef | null>`.
  Реализация поверх HTTP, подмена в тестах — другая функция того же типа, без загрузки весов.
  «Маски нет» остаётся штатным исходом: слой `cutout` снимается правилом K-3, карточка
  собирается без него. Тест на оба исхода. Не требует коробки.

- [ ] **4. Наряд для отдельного окна VS Code.** Сервис пишется в **другом репозитории и
  другом окне**, а значит — в сессии, которая этого плана не видит и историю Merch Kit не
  читает. Передавать ей «как обсуждали» нечего: нужен самодостаточный документ, по образцу
  наряда фазы B ([`m7-phase-b-handoff_2026-09-01.md`](m7-phase-b-handoff_2026-09-01.md)),
  написанного под «холодного» исполнителя.

  **Наряд живёт в репозитории сервиса** (`PLAN.md`), а не здесь: его читает тот, кто пишет
  сервис, и он должен лежать у него под рукой. Здесь остаётся план-контракт — что считается
  сделанным и почему приняты решения. Дублируются в наряд ровно три вещи, без которых сервис
  написать нельзя:
  - **контракт**: `POST /cutout` — кадр в теле, PNG-маска в ответе, `GET /health`; коды
    ошибок; «маски нет» — законный ответ, а не сбой;
  - **измеренные требования**: арена ORT выключена, модель резидентно, один запрос за раз,
    число потоков по ядрам коробки, ожидаемые 597 МБ и 12–24 с;
  - **граница доверия из ADR-0016**: общий секрет поверх TLS, никаких ключей проекта на
    коробке.

  Обратный поток тоже назначается здесь, иначе он не случится: когда сервис готов, в Merch
  Kit возвращаются **адрес, имя секрета и числа замера с коробки** — они идут в ADR-0015 и в
  шаг 8. Всё остальное знание остаётся в репозитории сервиса и в Merch Kit не протекает.

- [ ] **5. Репозиторий сервиса.** Поднять `ovr58/cutout_runner` из `project-template`
  (`TODO(пользователя)`: доступ или содержимое шаблона), поверх — сервис:
  - `GET /health` — жив ли процесс и загружена ли модель (для проверки после развёртывания);
  - `POST /cutout` — кадр в теле, маска PNG в ответе, ошибка — кодом, а не пустым телом;
  - модель **резидентно в процессе** (загрузка 5 с — не на запрос), арена выключена, число
    потоков из конфигурации по числу ядер коробки;
  - веса в git не идут — скачиваются установщиком по ссылке и лицензии из таблицы кандидатов
    (правило ADR-0014, пункт 4);
  - один запрос за раз: инференс занимает все ядра, и очередь честнее, чем конкуренция.

  **Коробка создана 2026-09-03.** `155.212.208.55`, Ubuntu 26.04 LTS, 1 ядро / 2 ГБ / 15 ГБ,
  Санкт-Петербург, 22 ₽/сутки с IPv4. Доступной памяти 1 607 МБ при измеренном пике инференса
  597 МБ — запас двукратный. Конфигурация выбрана младшая сознательно: упирались бы мы в
  память, а её хватает; ядра добавляются кнопкой, когда замер на коробке (шаг 7) покажет, что
  вырез мешает.

  **Сделано в тот же день — вход только по ключу.** Beget выдал пароль root и показал его на
  экране; пароль попал в переписку, то есть был скомпрометирован с самого начала. Найдена
  ловушка в раскладке `sshd`: drop-in `50-cloud-init.conf` включает `PasswordAuthentication
  yes` и **побеждает** `60-cloudimg-settings.conf`, где стоит `no`, — в `sshd` выигрывает
  первое встреченное значение, а не последнее. Поэтому заведён свой
  `/etc/ssh/sshd_config.d/00-hardening.conf`, читаемый раньше всех; чужие файлы не правились.
  `PermitRootLogin` пока `prohibit-password`, а не `no`: вход root по ключу сейчас
  единственный. Проверено новой сессией до закрытия старой — ключ пускает, пароль отвергнут
  (`Permission denied (publickey)`), то есть утёкший пароль стал бесполезен.

  **Защита коробки закрыта 2026-09-04.** Каждое изменение проверялось новой сессией, а не
  чтением конфигурации:

  | Что | Состояние | Чем проверено |
  | --- | --- | --- |
  | Вход по паролю | выключен | попытка входа отвергнута: `Permission denied (publickey)` |
  | Вход root | `PermitRootLogin no` | попытка отвергнута |
  | Административный пользователь | `mkit`, группа `sudo`, вход по тому же ключу | вход и `sudo -n id` дают `uid=0` |
  | Firewall | `ufw`: входящие запрещены, открыты 22, 80, 443 | `ufw status verbose`, доступ жив после включения |
  | Перебор паролей | `fail2ban`, тюрьма `sshd` | `fail2ban-client status` |
  | Обновления безопасности | `unattended-upgrades` | `APT::Periodic::Unattended-Upgrade "1"` |
  | Имя хоста | `cutout-runner` вместо случайного | `hostnamectl` |
  | Что слушает наружу | **только sshd** | `ss -tlnp` |

  Две вещи записаны с причиной, чтобы потом не выглядели небрежностью. **`sudo` без пароля**
  для `mkit`: пароля у пользователя нет вовсе, и обычный `sudo` спрашивал бы то, чего не
  существует; так же устроены облачные образы Ubuntu, а граница доступа здесь — SSH-ключ.
  **Порт SSH не менялся** — смена порта прячет от шумных сканеров, но не защищает, а цену
  берёт настоящую: её надо помнить всем и всегда.

  **Осталось по шагу:** nginx с TLS от Let's Encrypt — ждёт делегирования домена в зоне `.ru`;
  ограничения systemd приедут вместе с сервисом (шаг 7).

  **Домен куплен 2026-09-04: `mekit.ru`.** У серверов имён Beget A-запись уже указывает на
  `155.212.208.55`; в зоне `.ru` делегирование на момент записи ещё не разошлось (публичные
  резолверы отвечают NXDOMAIN — это ожидание реестра, а не ошибка). Поддомен сервиса —
  `cutout.mekit.ru`, `TODO(пользователя)`: завести A-запись. Корень домена остаётся под
  будущий прод.

- [ ] **6. Базовая защита коробки — только бесплатным.** По SSH, по одному изменению за раз,
  каждое проверяется до перехода к следующему; порядок такой, чтобы не отрезать себе доступ:
  - отдельный пользователь с `sudo`, вход по ключу; `PasswordAuthentication no`,
    `PermitRootLogin no`; **проверка новой сессией до закрытия старой**;
  - `ufw`: входящие запрещены, открыты 22, 80, 443 — и больше ничего;
  - `fail2ban` на `sshd` с настройками по умолчанию;
  - `unattended-upgrades` на security-обновления;
  - сервис слушает **только `127.0.0.1`**; наружу — исключительно через nginx;
  - nginx: TLS от Let's Encrypt с автопродлением, только TLS 1.2+, HSTS, потолок размера тела,
    ограничение частоты на `/cutout`;
  - systemd: `NoNewPrivileges`, `ProtectSystem=strict`, `PrivateTmp`, `MemoryMax` с запасом
    над измеренными 600 МБ, перезапуск с потолком частоты.
  Смену порта SSH и прочую безопасность-через-неизвестность **не делаем**: шума много,
  защиты ноль. Бэкапы у Beget автоматические и бесплатные, но опираться на них нечем —
  коробка не хранит состояния и воспроизводится установщиком.

- [ ] **7. Развёртывание и первый прогон на коробке.** Установка по `deploy/README.md` на
  чистую машину, `GET /health` снаружи по TLS, `POST /cutout` с тем же кадром, что гонялся
  локально, — маска обязана совпасть. Затем **тот же замер, что делался локально**
  (`arena-off`, число потоков коробки): числа с коробки возвращаются в ADR-0015 и в план
  фазы B, потому что именно они — настоящие, а мои локальные были верхней оценкой.

- [ ] **8. Интеграция.** Edge Function зовёт сервис через `CutoutRunner`; адрес и секрет —
  конфигурацией, не в коде. Пока коробки нет или она недоступна, поведение штатное: маски
  нет → слой снимается K-3. Проверить оба пути на локальном стенде.

## Verification

- `npm test` и `npm run lint` в Merch Kit — зелено; новый тест шва покрывает оба исхода
  («маска есть» и «маски нет»).
- `GET https://<поддомен>/health` снаружи отвечает 200 по валидному сертификату; та же
  проверка по `http://` даёт редирект на TLS.
- `POST /cutout` **без секрета** и с неверным секретом отвечает 401, а не маской.
- Порт сервиса снаружи закрыт: `ss -tlnp` на коробке показывает `127.0.0.1`, а сканирование
  порта снаружи — отказ.
- `sshd`: вход по паролю невозможен, вход root невозможен — проверено попыткой, а не чтением
  конфигурации.
- Пик памяти процесса под нагрузкой ниже `MemoryMax`; арена выключена — подтверждается
  измеренным пиком около 600 МБ, а не чтением кода.
- Маска с коробки совпадает с локальной на том же кадре; числа замера записаны в ADR-0015.
- Генерация карточки при **выключенном** сервисе доходит до конца: слой `cutout` снят по K-3,
  ошибки пользователю нет.

## Открытые вопросы

- `TODO(пользователя)`: тариф VPS — подтвердить рекомендацию 2 vCPU / 4 ГБ или назвать другой.
- `TODO(пользователя)`: имя домена и имя поддомена под сервис (прод потом на том же домене).
- Репозиторий сервиса [`ovr58/cutout_runner`](https://github.com/ovr58/cutout_runner) **публичный и
  пуст** — проверено 2026-09-03. Публичность не мешает, но задаёт правило: в репозиторий не идут ни
  секрет вызывающего, ни адрес коробки, ни веса модели. Конфигурация развёртывания лежит там
  примером с подставными значениями, реальные живут в файле окружения на коробке.
- `TODO(пользователя)`: доступ к `project-template` — проще всего клонировать его локально самому и
  назвать путь; тогда токены в диалоге не участвуют вовсе.
- Сборка карточки на этой же коробке — **не в этом плане**: ADR-0015 требует одного сервиса с
  двумя операциями, но перенос сборщика принадлежит шагу B7. Здесь закладывается место, а не
  реализация.
- Пересылка байтов кадра вместо ссылки на Storage выбрана ради того, чтобы у коробки не было
  ключей проекта. Если кадры вырастут настолько, что это станет дорого, решение
  пересматривается отдельным ADR, а не правкой кода.
