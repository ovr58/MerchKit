# Контрольная выборка

Пусто до появления реальных фото. Стенд на пустом каталоге падает намеренно: прогон «ни на
чём» дал бы зелёный отчёт и ложную уверенность.

> TODO(пользователь): 10–15 наборов фото **реальных** товаров — по 1–4 ракурса на набор
> (FR-02), покрывающих категории [`docs/TZ.md`](../../docs/TZ.md) §5.1, включая «Прочее».
> Нужны не стоки, а честные пользовательские снимки: блики, фон комнаты, случайный ракурс —
> рынок ломается именно на них. Обязательно заведомо трудные: мелкий предмет, зеркальная
> поверхность, текст на упаковке.

Один набор — один каталог: `bench/samples/<id>/` с фото и `sample.json`. Формат манифеста и
смысл полей — [`../README.md`](../README.md).

Выборка идёт в git вместе с фото: сравнение кандидатов имеет смысл, только если вход у них
буквально один и тот же и не дрейфует между прогонами.
