/**
 * Перечень категорий товара — TZ.md §5.1, фиксированный семь штук. `id` совпадает с
 * `public.categories.id` (справочник заводится миграцией `20260829100000_taxonomy.sql`);
 * совпадение — конвенция, не проверяется здесь в рантайме.
 *
 * Общий источник для заглушки (нужен только перечень id, воспроизводимо выбрать один по
 * хешу) и реального клиента (нужны ещё и русские названия — вставляются в промпт
 * распознавания, чтобы модель отвечала строго id, а не переизобретала список).
 */
export const CATEGORY_TITLES: Record<string, string> = {
  clothing: 'Одежда и обувь',
  accessories: 'Аксессуары',
  food: 'Еда и напитки',
  beauty: 'Косметика и уход',
  tech: 'Гаджеты и техника',
  home: 'Дом и мебель',
  other: 'Прочее',
}

export const CATEGORY_IDS = Object.keys(CATEGORY_TITLES)
