-- Первая миграция вехи M1. Доменных таблиц здесь нет и быть не должно: M1 — каркас.
-- Её работа — доказать, что конвейер миграций накатывается на пустую базу, и завести
-- схему, в которой с M2 будет жить единая SQL-функция условия доступа (docs/SPEC.md §4).

create schema if not exists app;

comment on schema app is
  'Собственные функции и типы Merch Kit. Пользовательские таблицы остаются в public под RLS.';
