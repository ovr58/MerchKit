# Единый перевод строки в репозитории: в git и в рабочем дереве — LF.
# Без этого Windows-клиенты добавляют CRLF и дифф забивается «изменениями» всего файла.
* text=auto eol=lf

# Бинарное — не трогать.
*.png binary
*.jpg binary
*.pdf binary
*.svg text eol=lf
