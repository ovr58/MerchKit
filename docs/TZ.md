# ТЗ — техническое задание

Status: DRAFT (черновик интейка от 2026-08-26) · Слой: **что и зачем**

> **Это черновик, собранный из `docs/Техническое задание.pdf` (вход docs-first), а не готовый
> документ.** Всё, что помечено `> TODO(пользователь):`, — место, где в исходном ТЗ ответа
> не было; оно **не додумано** намеренно. Правь прямо здесь: этот файл — источник требований
> для всей дальнейшей работы.
>
> **Куда что:** «как это устроено» — в [`SPEC.md`](SPEC.md) · схемы и картинки —
> в [`VISUALS.md`](VISUALS.md) (ссылаться, не копировать) · термины — в
> [`../CONTEXT.md`](../CONTEXT.md) · сокращения — в [`GLOSSARY.md`](GLOSSARY.md) ·
> необратимые решения — в [`adr/`](adr/README.md) · отложенное — в
> [`../planning/BACKLOG.md`](../planning/BACKLOG.md).

---

## 1. Цель

Merch Kit — веб-сервис, в котором продавец загружает обычное фото своего товара и получает
готовый продающий контент: набор студийных изображений товара в выбранном сценарии показа
(«на модели», «как в магазине», «раскладка сверху», «каталог») или целиком карточку товара —
карусель изображений вместе с заголовком и описанием. Все генерации складываются в личный
каталог, куда можно вернуться и скачать материалы повторно. Оплата — внутренними баллами.

**Проблема, которую это снимает:** сегодня, чтобы получить приличные фото товара для
маркетплейса или соцсетей, продавцу нужны съёмка, модель, студия или дизайнер — это дни
ожидания и заметные деньги за одну карточку. Альтернатива — снять на телефон, и карточка
проигрывает конкурентам.

**Метрика успеха:**

> TODO(пользователь): в исходном ТЗ метрики нет. Нужен измеримый признак — например «доля
> генераций, изображения которых пользователь скачал, ≥ N %» (скачивание = результат
> оказался годным) или «время от загрузки фото до скачанного результата ≤ N минут».

**Референс:** [aidentika.com](https://www.aidentika.com/) — разбор в [V-05](VISUALS.md#v-05).

## 2. Пользователи и роли

| Роль | Кто это | Главная задача в системе |
| --- | --- | --- |
| **Гость** | Не зарегистрирован, открыл сайт | Понять, что делает сервис; собрать генерацию и упереться в требование регистрации |
| **Продавец** (главная роль) | Зарегистрированный пользователь: селлер маркетплейса, владелец небольшого магазина, SMM-специалист | Загрузить фото товара и получить готовые изображения / карточку; вести каталог своих генераций; следить за балансом |

**Главная роль:** Продавец — по ней строится главный сценарий US-01.

> TODO(пользователь): нужна ли роль администратора (модерация, ручное начисление баллов,
> просмотр чужих генераций)? В исходном ТЗ её нет.

## 3. Scope и не-scope

### Входит в эту версию

- Каталог генераций пользователя (список созданного).
- Создание генерации: загрузка фото → автоопределение категории и товара → выбор типа
  генерации → выбор сценария показа → свободные пожелания → выбор количества объектов →
  запуск → результат → скачивание.
- Два типа генерации: **фото** и **карточка** (карусель изображений + заголовок + описание).
- Семь категорий товара с предсозданными сценариями показа (по 4 на категорию, кроме
  «Прочее»).
- Регистрация / авторизация по email + паролю, восстановление пароля.
- Стартовое начисление баллов при регистрации.
- Профиль: баланс в баллах, пополнение выбором пакета, выход из аккаунта.

### Сознательно НЕ входит (не-scope)

- **Эквайринг и реальные платежи.** Прямо зафиксировано в ТЗ: после выбора пакета баллы
  зачисляются автоматически, без оплаты. Значит и чеков, возвратов, платёжного провайдера,
  налоговой отчётности в этой версии нет.
- **Генерация видео.** В референсе aidentika третья вкладка «Видео» есть — в нашем ТЗ типов
  генерации ровно два, «фото» и «карточка». Видео не делаем.
- **Соцвход (Google / Apple / VK).** ТЗ называет ровно один способ входа: email + пароль.
- **Публикация карточки на маркетплейс.** Пользователь скачивает материалы и раскладывает
  их сам; интеграций с Ozon / WB / Avito нет.
- **Редактор изображений.** Результат берётся как есть; ретуши, кропа, замены фона руками
  в этой версии нет — только перезапуск генерации.
- **Командная работа.** Один аккаунт = один пользователь; общих рабочих пространств нет.

> Отложенное с ценностью на будущее — в [`../planning/BACKLOG.md`](../planning/BACKLOG.md).

## 4. Сценарии

### US-01 — Создать генерацию по фото товара (главный)

**Как** продавец, **я хочу** загрузить фото своего товара и получить готовые изображения в
выбранном сценарии показа, **чтобы** оформить карточку товара без съёмки и дизайнера.

Поток — схема [V-01](VISUALS.md#v-01). Состояния экранов — [V-06](VISUALS.md#v-06).

Критерии приёмки:

- **Given** я на главной, **When** я перетаскиваю фото товара в зону загрузки, **Then**
  фото отображается миниатюрой и становится доступен шаг «Настройте генерацию».
- **Given** фото загружено, **When** система его обработала, **Then** в полях «Это»
  подставлены определённые ИИ название товара (например «Куртка») и категория (например
  «Одежда и обувь»), **и** оба поля доступны для ручного исправления.
- **Given** категория определена и это не «Прочее», **When** я открываю «Как показать товар»,
  **Then** мне показаны ровно 4 предсозданных сценария этой категории с превью и подписью.
- **Given** категория «Прочее», **When** я открываю «Как показать товар», **Then**
  предсозданных сценариев нет, и доступно только поле свободного ввода пожеланий.
- **Given** я выбрал тип генерации, сценарий и количество объектов, **When** количество
  меняется, **Then** отображаемая цена в баллах пересчитывается сразу же.
- **Given** я не авторизован, **When** я нажимаю «Запустить генерацию», **Then** генерация
  не запускается, а мне предлагается зарегистрироваться (US-02), **и** собранные настройки
  не теряются — после регистрации я возвращаюсь к ним.
- **Given** я авторизован и баллов хватает, **When** я запускаю генерацию, **Then** с баланса
  списывается цена генерации, показывается прогресс, а по завершении в блоке «Результаты»
  появляются изображения в заказанном количестве.
- **Given** генерация завершена, **When** я нажимаю скачивание, **Then** изображения
  сохраняются на мой компьютер в полном разрешении.
- **Given** генерация завершена, **When** я открываю каталог генераций, **Then** эта
  генерация присутствует в списке под названием, которое дал ИИ.
- **Given** тип генерации «карточка», **When** генерация завершена, **Then** результат
  содержит карусель изображений, заголовок и описание товара.

### US-02 — Зарегистрироваться и получить стартовые баллы

**Как** гость, **я хочу** завести аккаунт, **чтобы** запускать генерации.

Критерии приёмки:

- **Given** я на форме регистрации, **When** я ввожу email, пароль и подтверждение пароля и
  подтверждение совпадает с паролем, **Then** аккаунт создаётся и я авторизован.
- **Given** подтверждение пароля не совпадает с паролем, **When** я отправляю форму, **Then**
  аккаунт не создаётся и показана ошибка у поля подтверждения.
- **Given** email уже зарегистрирован, **When** я отправляю форму, **Then** аккаунт не
  создаётся и показано понятное сообщение со ссылкой на вход.
- **Given** регистрация прошла, **When** я открываю профиль, **Then** на балансе N баллов.

> TODO(пользователь): **N — сколько баллов даём при регистрации?** В ТЗ стоит буква N.
> TODO(пользователь): требуется ли подтверждение email по ссылке из письма до первой
> генерации, или аккаунт активен сразу?

### US-03 — Войти и восстановить пароль

**Как** продавец, **я хочу** войти по email и паролю и восстановить доступ, если пароль
забыт, **чтобы** не потерять свой каталог генераций и баллы.

Критерии приёмки:

- **Given** у меня есть аккаунт, **When** я ввожу верные email и пароль, **Then** я авторизован
  и вижу свой каталог генераций.
- **Given** я забыл пароль, **When** я запрашиваю восстановление по своему email, **Then** мне
  приходит письмо со ссылкой на смену пароля.
- **Given** я перешёл по ссылке из письма, **When** я задаю новый пароль, **Then** старый
  пароль перестаёт работать, а новый — работает.

### US-04 — Вернуться в свою генерацию и скачать материалы повторно

**Как** продавец, **я хочу** открыть ранее сделанную генерацию из каталога, **чтобы**
скачать её материалы ещё раз, не платя повторно.

Критерии приёмки:

- **Given** у меня есть завершённые генерации, **When** я открываю каталог, **Then** я вижу
  их списком с названием от ИИ и превью.
- **Given** я открываю генерацию из каталога, **When** она загрузилась, **Then** мне доступны
  её изображения (и для «карточки» — заголовок с описанием) и скачивание, **и** баллы за это
  не списываются.

### US-05 — Пополнить баланс баллов

**Как** продавец, **я хочу** выбрать пакет баллов, **чтобы** продолжить генерировать, когда
баллы кончились.

Критерии приёмки:

- **Given** я в профиле, **When** страница открылась, **Then** я вижу текущий баланс в баллах.
- **Given** я выбираю пакет, **When** я подтверждаю выбор, **Then** баллы пакета немедленно
  зачисляются на баланс без шага оплаты (эквайринга в этой версии нет), **и** операция видна
  в истории баланса.
- **Given** я нажимаю «Выйти», **When** выход выполнен, **Then** я снова гость и мой каталог
  генераций мне недоступен.

> TODO(пользователь): состав пакетов — сколько баллов и по какой номинальной цене в каждом.

### Сценарии ошибок

| ID | Что пошло не так | Что должна сделать система |
| --- | --- | --- |
| US-E1 | Загружен файл неподдерживаемого формата или слишком большой | Не принимать файл, назвать допустимые форматы и предельный размер |
| US-E2 | ИИ не смог определить категорию / товар по фото | Не блокировать сценарий: оставить поля пустыми и предложить выбрать категорию и указать товар вручную |
| US-E3 | Баллов на балансе меньше цены генерации | Не запускать генерацию и не списывать баллы; показать, сколько не хватает, и увести на пополнение |
| US-E4 | Провайдер генерации недоступен или вернул ошибку | Вернуть списанные баллы, показать понятную ошибку и предложить повторить; генерация помечается как неуспешная и не засоряет каталог как «готовая» |
| US-E5 | Генерация выполнена частично (часть объектов не получилась) | Показать полученные объекты и списать баллы только за них; за неполученные — вернуть |
| US-E6 | Сессия истекла во время настройки генерации | Сохранить собранные настройки, предложить войти и вернуть на тот же шаг |
| US-E7 | Восстановление пароля запрошено для незарегистрированного email | Отвечать одинаково вне зависимости от наличия аккаунта — не раскрывать, зарегистрирован ли email |

## 5. Функциональные требования

| ID | Требование | Зачем (rationale) | Fit criterion (как проверить) | Сценарий |
| --- | --- | --- | --- | --- |
| FR-01 | Система отображает авторизованному пользователю список его ранее созданных генераций | Ради этого пользователь возвращается: каталог — его актив | В каталоге присутствуют все завершённые генерации пользователя и отсутствуют чужие | US-04 |
| FR-02 | Система принимает от пользователя загрузку фотографий товара для генерации | Фото — единственный вход в сценарий | Файл принят drag-and-drop и выбором из проводника; принятый файл виден миниатюрой | US-01 |
| FR-03 | Система автоматически определяет категорию товара по загруженному фото из фиксированного перечня семи категорий | Иначе пользователь выбирает категорию руками при каждой генерации | На контрольной выборке фото определённая категория совпадает с ожидаемой; определённое значение всегда принадлежит перечню §5.1 | US-01 |
| FR-04 | Система автоматически определяет наименование товара по загруженному фото (например «куртка») | Наименование идёт в промпт генерации и в текст карточки | Поле «Это» заполнено непустым наименованием после обработки фото | US-01 |
| FR-05 | Система позволяет пользователю исправить автоматически определённые категорию и наименование | Автоопределение ошибается, и тупик здесь ломает весь сценарий | Изменённые вручную значения используются при генерации вместо определённых автоматически | US-01, US-E2 |
| FR-06 | Система предоставляет выбор типа генерации из двух вариантов: «фото» и «карточка» | Разные результаты: набор изображений против готовой карточки | В интерфейсе доступны ровно два типа; выбор влияет на состав результата | US-01 |
| FR-07 | При типе генерации «карточка» система формирует карусель изображений, заголовок и описание товара | Это и есть отличие «карточки» от «фото» | Результат генерации типа «карточка» содержит все три части | US-01 |
| FR-08 | Для каждой категории, кроме «Прочее», система отображает 4 предсозданных варианта сценария показа товара | Пользователь не умеет писать промпты — выбор из готовых снимает эту задачу | Для каждой из шести категорий отображается ровно 4 варианта; для «Прочее» — ни одного | US-01 |
| FR-09 | Система предоставляет поле свободного ввода пожеланий к генерации | Готовые сценарии не покрывают всё; для «Прочее» это единственный способ задать сценарий | Введённый текст влияет на результат генерации | US-01 |
| FR-10 | Система позволяет выбрать количество генерируемых объектов | Пользователю нужен выбор между «дешевле» и «больше вариантов» | Количество объектов в результате равно выбранному | US-01 |
| FR-11 | Система рассчитывает цену генерации в баллах в зависимости от выбранного количества объектов и показывает её до запуска | Списание без предварительно показанной цены — потеря доверия | При изменении количества отображаемая цена пересчитывается; списанная сумма равна показанной | US-01 |
| FR-12 | Система запускает генерацию только для авторизованного пользователя, а неавторизованному предлагает регистрацию | Списывать баллы не с кого, и результат некуда сохранить | Гость, нажавший «Запустить генерацию», видит предложение регистрации, генерация не стартует | US-01, US-02 |
| FR-13 | Система списывает цену генерации с баланса пользователя при запуске и возвращает списанное при неуспехе | Баллы — валюта продукта; ошибка списания = прямой ущерб пользователю | После неуспешной генерации баланс равен балансу до запуска | US-01, US-E4, US-E5 |
| FR-14 | Система отображает пользователю результат генерации | — | По завершении изображения видны в блоке результатов без перезагрузки страницы | US-01 |
| FR-15 | Система позволяет скачать полученные изображения на компьютер пользователя | Материалы нужны за пределами сервиса | Скачанный файл открывается и соответствует показанному изображению | US-01, US-04 |
| FR-16 | Система сохраняет завершённую генерацию в каталог пользователя под названием, сгенерированным ИИ | Список из «Генерация №17» нечитаем | У каждой завершённой генерации непустое осмысленное название, полученное автоматически | US-01, US-04 |
| FR-17 | Система позволяет повторно открыть генерацию из каталога и скачать её материалы без повторного списания баллов | Пользователь платит за генерацию, а не за доступ к своему результату | Повторное скачивание не меняет баланс | US-04 |
| FR-18 | Система регистрирует пользователя по email, паролю и подтверждению пароля | Единственный способ входа по ТЗ | Аккаунт создаётся только при совпадении пароля и подтверждения | US-02 |
| FR-19 | Система начисляет новому пользователю N баллов после регистрации | Дать попробовать продукт до пополнения | Баланс сразу после регистрации равен N | US-02 |
| FR-20 | Система аутентифицирует пользователя по email и паролю | — | С верной парой вход выполняется, с неверной — нет | US-03 |
| FR-21 | Система предоставляет восстановление пароля по email | Забытый пароль иначе означает потерю каталога и баллов | По ссылке из письма пароль меняется; старый перестаёт действовать | US-03 |
| FR-22 | Система отображает в профиле текущий баланс пользователя в баллах | — | Отображаемое значение совпадает с фактическим балансом | US-05 |
| FR-23 | Система позволяет пополнить баланс выбором пакета баллов, зачисляя баллы немедленно и без оплаты | Эквайринг в этой версии сознательно не подключается | После выбора пакета баланс увеличен на номинал пакета; платёжных шагов нет | US-05 |
| FR-24 | Система позволяет пользователю выйти из аккаунта | — | После выхода защищённые разделы недоступны | US-05 |

### 5.1 Перечень категорий товара (фиксированный)

1. Одежда и обувь
2. Аксессуары
3. Еда и напитки
4. Косметика и уход
5. Гаджеты и техника
6. Дом и мебель
7. Прочее — **без** предсозданных сценариев показа (только свободный ввод)

Итого предсозданных сценариев: 6 категорий × 4 = **24**.

> TODO(пользователь): нужны формулировки всех 24 сценариев. Из референса подтверждены четыре
> для «Одежда и обувь»: «На модели» (носимый контекст, акцент на посадке), «Как в магазине»
> (на вешалке или подставке), «Раскладка сверху» (вид строго сверху), «Каталог (студийно)»
> (чистый объект на нейтральном фоне) — см. [V-05](VISUALS.md#v-05).

## 6. Нефункциональные требования

| ID | Категория | Требование | Fit criterion |
| --- | --- | --- | --- |
| NFR-01 | Производительность (интерфейс) | Автоопределение категории и товара не должно ощущаться как зависание | Результат распознавания показан ≤ 5 с после загрузки фото либо сопровождается индикатором прогресса |
| NFR-02 | Производительность (генерация) | Генерация — долгая операция и обязана иметь видимый прогресс и не рваться при перезагрузке страницы | Статус генерации восстанавливается после F5; пользователь видит стадию, а не «замерший» экран |
| NFR-03 | Надёжность биллинга | Списание и возврат баллов не теряются и не дублируются при сбое | Повторная доставка одного и того же события генерации не меняет баланс дважды (идемпотентность) |
| NFR-04 | Безопасность данных | Пользователь не может читать и изменять чужие генерации, файлы и баланс | Прямой запрос к API / хранилищу с чужим идентификатором возвращает отказ; проверено на уровне БД, а не только UI |
| NFR-05 | Безопасность (баланс) | Баланс изменяется только сервером; клиент не может начислить себе баллы | Попытка изменить баланс клиентским запросом отклоняется |
| NFR-06 | Приватность | Персональные данные: email и пароль (хеш); пользовательский контент: фото товаров | Хранилище и срок хранения зафиксированы в `SPEC.md` §4; пароли в открытом виде не хранятся |
| NFR-07 | Доступность (a11y) | Целевой уровень WCAG | > TODO(пользователь): A / AA / AAA? |
| NFR-08 | Интернационализация | Интерфейс на русском языке | Все строки интерфейса на русском; > TODO(пользователь): нужны ли другие локали |
| NFR-09 | Адаптивность | Сценарий работает на мобильном браузере, а не только на десктопе | > TODO(пользователь): подтвердить — референс десктопный, трёхколоночный |
| NFR-10 | Наблюдаемость | Видны события: регистрация, запуск генерации, списание / возврат баллов, ошибка провайдера | Перечисленные события присутствуют в логах с идентификатором генерации |
| NFR-11 | Объёмы | Ожидаемое число пользователей и генераций в сутки | > TODO(пользователь): нет данных — влияет на выбор тарифа Supabase и лимиты провайдера |

## 7. Данные и приватность

- **Что хранится:** пользователи (email, хеш пароля), баланс и история операций с баллами,
  генерации (входные фото, параметры, статус, название от ИИ), результаты генераций
  (изображения; для «карточки» — заголовок и описание), справочники категорий и сценариев
  показа, пакеты пополнения.
- **Откуда берётся:** входные фото — от пользователя; категория, наименование, изображения,
  название генерации, тексты карточки — от AI-провайдера; справочники — заводятся заранее.
- **Персональные данные:** есть — email. Плюс пользовательский контент (фото товаров),
  который может быть коммерческой тайной пользователя.
- **Цена потери данных:** потеря каталога генераций — тяжело: пользователь заплатил баллами
  за результат, восстановить его без повторной генерации нельзя. Потеря баланса —
  критично. Входные фото — переживём, их можно загрузить заново.

Модель данных — схема [V-04](VISUALS.md#v-04), детали хранения — `SPEC.md` §4.

> TODO(пользователь): сколько храним изображения генераций — бессрочно или N месяцев?
> Это прямо влияет на стоимость хранилища.

## 8. Ограничения

| Ограничение | Источник | Следствие для решения |
| --- | --- | --- |
| Стек зафиксирован: React.js + Supabase | ТЗ заказчика | Фронтенд — React; аутентификация, БД, файлы и серверная логика — в Supabase, отдельный бэкенд-сервис не заводим |
| Эквайринг не подключаем | ТЗ заказчика | Пополнение — фиктивное, мгновенное зачисление; платёжный провайдер не выбирается и не интегрируется |
| Вход только email + пароль | ТЗ заказчика | OAuth-провайдеры не подключаем |
| Типов генерации ровно два: «фото» и «карточка» | ТЗ заказчика | Видео из референса не реализуем |
| Категорий ровно семь, сценариев по 4 на категорию кроме «Прочее» | ТЗ заказчика | Справочник фиксированный; произвольные категории пользователь не заводит |
| Ключи AI-провайдера нельзя раскрывать клиенту | Безопасность | Вызов провайдера — только с серверной стороны (Edge Function), не из браузера |

## 9. Критерии приёмки релиза

Релиз считается состоявшимся, когда:

- [ ] Все критерии `Given/When/Then` сценариев US-01…US-05 проходят.
- [ ] Все сценарии ошибок US-E1…US-E7 воспроизведены и отработаны как описано.
- [ ] Все FR-01…FR-24 проверены по своим fit criterion.
- [ ] NFR-03 (идемпотентность биллинга) и NFR-04 / NFR-05 (изоляция чужих данных и баланса)
      проверены отдельно — это места, где ошибка стоит денег и доверия.
- [ ] Метрика успеха из §1 определена и измерена.
- [ ] Все 24 предсозданных сценария показа заведены и дают осмысленный результат.

> Проверка перед объявлением готовности — скил `verification-before-completion`.

## 10. Риски

| Риск | Вероятность | Цена, если случится | Что делаем заранее |
| --- | --- | --- | --- |
| Качество генерации ниже ожиданий: товар на выходе не похож на загруженный | высокая | Продукт не нужен: продавец не поставит чужую куртку в свою карточку | Провайдер выбирается по способности сохранять предмет с фото-референса; сохранение предмета — приёмочный критерий, а не «посмотрим потом» |
| Себестоимость генерации у провайдера выше, чем цена в баллах | средняя | Каждая генерация приносит убыток | Цену пакетов и цену генерации считать от фактической себестоимости вызова; заложить в SPEC учёт стоимости на генерацию |
| Автоопределение категории ошибается и уводит пользователя не в те сценарии | средняя | Плохой результат и потраченные баллы | FR-05: ручное исправление обязательно |
| Долгая генерация: пользователь уходит со страницы и теряет результат | высокая | Списанные баллы без результата — прямой конфликт | NFR-02: статус генерации живёт на сервере, страница его восстанавливает |
| Загрузка запрещённого контента под видом товара | средняя | Юридические претензии к сервису | > TODO(пользователь): нужна ли модерация загрузок и ответов провайдера |
| Отсутствие эквайринга притягивает накрутку баллов | средняя | Расход на генерации без выручки | Начисление только серверной логикой (NFR-05); > TODO(пользователь): ограничивать ли число фиктивных пополнений |

## 11. Открытые вопросы

- [ ] > TODO(пользователь): **N** — сколько баллов начисляем при регистрации (FR-19).
- [ ] > TODO(пользователь): цена генерации за один объект и её зависимость от количества;
      максимум объектов за раз.
- [ ] > TODO(пользователь): состав пакетов пополнения (номиналы).
- [ ] > TODO(пользователь): сколько фото товара загружается за раз — в ТЗ единственное число,
      в референсе «до 4 фото с разных сторон». Принято по референсу, требует подтверждения.
- [ ] > TODO(пользователь): сколько изображений в карусели для типа «карточка».
- [ ] > TODO(пользователь): формулировки всех 24 предсозданных сценариев показа (§5.1).
- [ ] > TODO(пользователь): подтверждение email при регистрации — обязательно или нет.
- [ ] > TODO(пользователь): нужна ли роль администратора.
- [ ] > TODO(пользователь): модерация загружаемого контента.
- [ ] > TODO(пользователь): срок хранения изображений генераций.
- [ ] > TODO(пользователь): целевой уровень a11y (NFR-07), локали (NFR-08), мобильная
      адаптивность (NFR-09), ожидаемые объёмы (NFR-11).
- [ ] AI-провайдер генерации — решение отложено пользователем; заложена абстракция провайдера,
      см. [ADR-0005](adr/0005-ai-provider-abstraction.md).

---

## Связанные документы

| Документ | Что там |
| --- | --- |
| [`SPEC.md`](SPEC.md) | Как это устроено: архитектура, стек, модули, окружения, handoff в планирование |
| [`VISUALS.md`](VISUALS.md) | Все схемы и изображения — **единственный** визуальный артефакт |
| `Техническое задание.pdf` | Исходное ТЗ заказчика, из которого собран этот документ |
| [`../CONTEXT.md`](../CONTEXT.md) | Единый язык проекта |
| [`adr/`](adr/README.md) | Необратимые решения |
